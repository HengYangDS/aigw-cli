schema_version = 1
id = "change:publication-topology-contract"
intent = "Declare one local-first, dual-Forge publication topology using existing repository-native verification, installation, and CI surfaces."
subjects = [
    "repository:self",
]
scope = [
    "openspec/changes/publication-topology-contract/**",
    ".ethos/release.toml",
    "scripts/checks/governance/check-governance.sh",
]
invariants = []
acceptance = []
risks = []
authority_refs = []
permissions = [
    "repository.read",
    "work-lane.write",
]
hypotheses = []
dependencies = []
