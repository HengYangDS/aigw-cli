schema_version = 1
id = "change:controlled-markdown-links"
intent = "Restrict documentation link validation to repository-controlled Markdown so private Git runtime projections cannot affect source quality."
subjects = ["repository:aigw-cli"]
scope = [
  "openspec/changes/controlled-markdown-links/**",
  "tools/ci/main.go",
  "tools/ci/main_test.go",
]
invariants = [
  "all tracked Markdown remains link-checked",
  "private runtime and Git metadata never participate in repository source gates",
  "the source gate remains cross-platform and repository-native",
]
acceptance = [
  "link validation consumes the exact tracked Markdown set",
  "untracked and private Git Markdown cannot alter the gate result",
  "the complete static gate passes from a checkout with ETHOS runtimes present",
]
risks = ["tracked Markdown omission"]
authority_refs = ["user_instruction", "AGENTS.md", ".ethos/profile.toml"]
dependencies = []
