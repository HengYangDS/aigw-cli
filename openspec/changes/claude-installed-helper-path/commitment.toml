schema_version = 1
id = "change:claude-installed-helper-path"
intent = "Bind Claude Code credential retrieval to the installed AIGW executable so GUI and shell launches use one stable invocation authority."
subjects = ["repository:aigw-cli"]
scope = ["CHANGELOG.md", "openspec/specs/product-control-plane/spec.md", "openspec/changes/claude-installed-helper-path/**"]
invariants = ["credential_lookup_uses_the_installed_executable", "user_owned_claude_state_and_plaintext_tokens_remain_unchanged"]
acceptance = ["focused_claude_projection_tests_pass", "source_quality_and_strict_spec_checks_pass", "installed_black_box_projection_passes"]
risks = ["a_relocated_install_requires_reprojection"]
authority_refs = ["AGENTS.md", ".ethos/profile.toml", "openspec/specs/product-control-plane/spec.md"]
hypotheses = []
dependencies = []
