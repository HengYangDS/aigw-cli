## 1. Deterministic verification

- [ ] 1.1 Replace the public GitHub fallback request with a controlled transport
  and assert the exact request and CLI fallback.
- [ ] 1.2 Add an explicit doctor fixture for an enabled Codex adapter without a
  configured target.
- [ ] 1.3 Run the focused tests and two consecutive full race-coverage gates.

## 2. Source-bound evidence

- [ ] 2.1 Add a failing governance regression for missing or inconsistent source
  identity, raw coverage counts, derived percentage, and claim digest.
- [ ] 2.2 Correct the dated candidate observation from the reproducible committed
  source and refresh its claim digest.
- [ ] 2.3 Validate and archive this change, sign the evidence commit, and rerun
  exact-HEAD ETHOS proof before any accepted-root transition.
