## Why

The accepted product now completes the deferred-client onboarding journey:
after installing a supported client, selecting its Profile converges the
Adapter and projection without requiring unrelated Accounts or clients. These
accepted bytes are newer than rc.105 and need one immutable release identity.

## What Changes

- Publish the accepted onboarding convergence as AIGW `0.1.0-rc.106`.
- Advance the version authority and release chronicle from the accepted source.
- Publish one signed source and tag object with identical assets to both Forges.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

None. The behavior is already owned by the accepted
`profile-client-selection` and `progressive-team-onboarding` specifications.

## Impact

Only release identity, chronology, and this bounded release Change are
modified. No product behavior, dependency, compatibility path, Proxy coupling,
or Forge-specific implementation is introduced.
