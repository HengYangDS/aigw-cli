## Why

The accepted AIGW product is newer than `0.1.0-rc.109`: setup now gives an
actionable continuation when clients are deferred, Codex verification exercises
the configured client directly, and Linux proves the existing Secret Service
failure contract. These accepted bytes need one immutable release identity.

## What Changes

- Publish the accepted product as AIGW `0.1.0-rc.110`.
- Advance the version and release chronicle authorities from the accepted
  source.
- Publish one signed source and tag object, with identical release assets on
  GitLab and GitHub.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

None. The observable behavior is already owned by the archived
`aigw-guided-setup-sync-continuation`, `aigw-direct-codex-journey`, and
`linux-secret-service-evidence` Changes.

## Impact

Only release identity, chronology, and this bounded release Change are
modified. No provider, credential backend, client adapter, endpoint, runtime
dependency, compatibility path, or Forge-specific product logic is added.
