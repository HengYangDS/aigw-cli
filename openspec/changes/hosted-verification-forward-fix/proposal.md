# Hosted verification forward fix

Close the two exact hosted-verification gaps without changing product behavior:
materialize GitLab trust input as a file, and exercise the Windows portable
install path that does not use POSIX executable bits.
