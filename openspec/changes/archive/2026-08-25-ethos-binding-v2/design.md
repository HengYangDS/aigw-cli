## Decision

Use the current typed Commitment schema as the sole tracked repository binding.
Preserve the prior semantic content and add only the required explicit empty
collections. Do not retain the legacy carrier, a converter, or a fallback parser.
