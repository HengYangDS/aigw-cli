# Windows native gate scope

The Windows native job proves Windows compilation, execution, installation, and
uninstallation. Aggregate statement and branch coverage remains a source gate
on the canonical Linux executor; it is not repeated as an operating-system gate.
