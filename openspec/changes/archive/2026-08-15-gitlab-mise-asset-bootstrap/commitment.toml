schema_version = 1
id = "change:gitlab-mise-asset-bootstrap"
intent = "Bootstrap the exact repository-pinned mise Linux asset with checksum verification and bounded HTTP/1.1 transport, eliminating the nested installer download failure."
subjects = [
  "repository:self",
]
scope = [
  "openspec/changes/gitlab-mise-asset-bootstrap/**",
  ".config/ci/pipeline.cue",
  "tools/ci/projection_test.go",
  ".gitlab-ci.yml",
]
invariants = [
  "mise.toml remains the exact tool-version authority",
  ".config/ci/pipeline.cue remains the sole CI command authority",
  "GitLab and GitHub publication remain independent",
]
acceptance = [
  "generated GitLab Linux jobs download the exact official mise archive and checksum manifest",
  "the archive checksum is verified before extraction and installation",
  "all bootstrap requests use bounded HTTP/1.1 transport",
  "interrupted archive requests resume without discarding received bytes",
  "the bootstrap contains no installer-script or GitHub API dependency",
  "the complete local source and governance graph passes",
]
risks = [
  "an unsupported Linux architecture fails closed before download",
  "release asset layout or checksum-manifest format drift fails closed before execution",
]
authority_refs = []
hypotheses = []
dependencies = []
