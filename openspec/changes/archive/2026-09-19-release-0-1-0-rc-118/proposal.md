## Why

The accepted source contains the completed stable supply-chain refresh after
rc.117. Those accepted bytes need one immutable release identity so installed
users receive the verified dependency graph rather than an older release.

## What Changes

- Publish the accepted supply-chain refresh as AIGW `0.1.0-rc.118`.
- Advance the version authority and release chronicle from the accepted source.
- Publish one signed source and tag object with identical assets to both Forges.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

None. Existing product behavior and public contracts are unchanged.

## Impact

Only release identity, chronology, and this bounded release Change are modified.
No product behavior, compatibility path, or additional release mechanism is added.
