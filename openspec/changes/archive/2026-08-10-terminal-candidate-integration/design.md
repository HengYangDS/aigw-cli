# Design

Archived Changes remain the behavior authorities. This terminal Change owns
only integration: it covers the complete accumulated lane delta and adds the
single permission required to update the local candidate ref.

ETHOS must bind the candidate ref, proven lane HEAD, Lease generation, signed
transition, and complete scope. Drift in any bound input fails closed.
