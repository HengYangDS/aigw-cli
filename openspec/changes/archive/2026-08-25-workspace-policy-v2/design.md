## Decision

Use the seven-field current branch-role contract as the sole workspace policy.
`release_mirror = "accepted_ff"` makes local `main` and `dev` one accepted
product object after closeout, matching AIGW's existing exact-object publication
contract. `canonical_sibling_worktrees = true` makes the established sibling
checkout topology explicit.

Delete the retired transition row rather than retaining a compatibility carrier.
