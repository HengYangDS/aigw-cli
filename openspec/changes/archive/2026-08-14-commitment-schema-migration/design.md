# Design

Use the public ETHOS adoption migration as the sole producer. The migration
removes retired fields, retires the tracked local-state marker, and creates the
missing OpenSpec entrypoint without replacing existing nonempty bindings. A
receipt-bound Commitment rebind moves the Work Lane to this exact tree.
