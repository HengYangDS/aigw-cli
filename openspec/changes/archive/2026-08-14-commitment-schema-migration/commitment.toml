schema_version = 1
id = "change:commitment-schema-migration"
intent = "Migrate the repository Commitment and retire the tracked local-state marker without changing product behavior."
subjects = ["repository:aigw-cli"]
scope = [
  ".ethos/commitment.toml",
  ".ethos/state/.gitignore",
  "openspec/changes/commitment-schema-migration/**",
  "openspec/specs/README.md",
]
invariants = [
  "product_behavior_is_unchanged",
  "repository_authority_remains_portable",
  "private_state_remains_outside_tracked_source",
]
acceptance = [
  "retired_permissions_are_removed",
  "tracked_local_state_marker_is_retired",
  "openspec_entrypoint_is_present",
  "governed_plan_is_unblocked",
]
authority_refs = ["user_instruction", "AGENTS.md", ".ethos/profile.toml"]
dependencies = []
