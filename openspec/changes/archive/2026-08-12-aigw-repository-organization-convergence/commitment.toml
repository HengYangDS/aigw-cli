schema_version = 1
id = "change:aigw-repository-organization-convergence"
intent = "Unify AIGW repository-level organization, version SSOT, and quality surfaces without changing product boundaries."
subjects = ["repository:aigw-cli"]
scope = ["VERSION", "CHANGELOG.md", "CONTRIBUTING.md", "docs/**", ".config/**", "tools/**", "cmd/**", "internal/**", "openspec/**"]
invariants = [
  "one_product_version_source",
  "repository_native_quality_and_release_contract",
  "provider_independent_local_first_publication",
  "semantic_layout_and_documentation_are_consistent",
]
acceptance = [
  "version_ssot_is_machine_verified",
  "dev_to_main_closeout_is_governed",
  "cross_repository_contract_differences_are_justified",
]
authority_refs = ["user_instruction", "AGENTS.md", ".ethos/profile.toml"]
permissions = ["repository.read", "work-lane.write", "git.ref.compare-and-swap"]
dependencies = []
