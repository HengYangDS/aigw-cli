## 1. Contract

- [x] 1.1 Specify mutually exclusive profile and client selection and verify
      strict OpenSpec validation passes.

## 2. Implementation

- [x] 2.1 Reject combined selectors in connectivity and live verification
      before credential or network access; verify focused command tests pass.
- [x] 2.2 Remove the obsolete missing-client recovery instruction from the
      current configuration query; verify configuration tests pass.

## 3. Closeout

- [x] 3.1 Verify current source has no competing combined-selector behavior,
      validate the specification delta, and run the complete source gate.
- [x] 3.2 Confirm the final tree is ready for exact-HEAD proof, governed archive,
      landing, publication, and Work Lane retirement.
