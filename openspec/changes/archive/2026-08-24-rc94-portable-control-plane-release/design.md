## Decision

Create prerelease rc.94 from the exact accepted portable control-plane product.
Keep `VERSION` as the version authority and `CHANGELOG.md` as the forward release
chronicle.

After source, native journey, and reproducible artifact proof pass, archive this
Change before constructing the signed product commit. Create one annotated tag
locally and publish the unchanged commit, tag, and asset set independently to
GitLab and GitHub.
