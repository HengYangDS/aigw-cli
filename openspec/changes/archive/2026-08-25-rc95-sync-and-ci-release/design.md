## Decision

Create prerelease rc.95 from the exact verified post-rc.94 product. Keep
`VERSION` as the version authority and `CHANGELOG.md` as the forward release
chronicle. A release identity is never reused for different source or artifact
bytes.

After source, native journey, and reproducible artifact proof pass, create one
annotated tag locally and publish the unchanged commit, tag, and asset set
independently to GitLab and GitHub.
