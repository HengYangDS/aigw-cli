## Why

The accepted post-rc.92 correction makes a partially connected team catalogue
healthy when every Account selected by an active client Route has its Token.
The published and installed product must expose those exact verified bytes.

## What Changes

Publish the routed-account diagnostic correction as rc.93 by advancing the
version source, recording the user-visible fix once, and validating the existing
release contract without changing provider, credential, client, or projection
semantics.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

None. The product behavior was specified and verified by the preceding bounded
Change; this Change owns release identity and chronology only.

## Impact

Only `VERSION`, `CHANGELOG.md`, and this bounded release Change are modified.
One signed product object and annotated tag will be projected unchanged to both
Forge peers.
