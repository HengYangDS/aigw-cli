## 1. Release epoch authority

- [x] 1.1 Add a regression proving local and tagged resolution use the same
  committed Changelog date for one formal version.
- [x] 1.2 Remove caller-selected version and epoch inputs from the public local
  build command; missing version chronology fails closed.
- [x] 1.3 Pass focused release tests and OpenSpec strict validation.

## 2. Reproducible delivery proof

- [x] 2.1 Build the current version through local and CI-shaped entry paths and
  prove the complete artifact matrices are byte-identical.
- [x] 2.2 Pass the complete source gate.
- [x] 2.3 Archive this Change before accepted-branch promotion.
