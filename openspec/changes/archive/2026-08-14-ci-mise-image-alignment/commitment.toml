schema_version = 1
id = "change:ci-mise-image-alignment"
intent = "Align the hermetic GitLab CI image with the repository-locked mise runtime so Linux verification executes instead of failing during tool bootstrap."
subjects = [
  "repository:self",
]
scope = [
  "openspec/changes/ci-mise-image-alignment/**",
  ".config/ci/**",
  ".gitlab-ci.yml",
  "tools/ci/**",
]
invariants = [
  "mise_toml_remains_the_toolchain_version_authority",
  "forge_projections_remain_generated_from_one_ci_model",
]
acceptance = [
  "gitlab_container_jobs_refresh_the_official_mise_runtime_before_locked_execution",
  "the_ci_projection_test_prevents_stale_container_bootstrap_from_returning",
]
risks = ["upstream_container_publication_can_lag_the_latest_stable_mise_release"]
authority_refs = ["mise.toml", ".config/ci/pipeline.cue", "AGENTS.md"]
hypotheses = []
dependencies = []
