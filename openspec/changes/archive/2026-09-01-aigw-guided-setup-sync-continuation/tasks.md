## 1. Guided setup continuation

- [x] 1.1 Add an acceptance assertion that setup without an installed client
      identifies `aigw sync`, not `aigw check`, as the next action; verify the
      focused test fails before implementation.
- [x] 1.2 Derive the continuation from configured client state without adding a
      second state owner; verify the focused onboarding and acceptance tests pass.

## 2. Verification and closeout

- [x] 2.1 Run the affected static and behavioral gates.

Exact-HEAD proof, archive, candidate integration, hosted CI, publication, and
Work Lane retirement are lifecycle effects performed after this editable
Change is complete.
