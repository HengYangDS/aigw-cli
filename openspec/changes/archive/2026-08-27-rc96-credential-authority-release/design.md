## Decision

Create rc.96 from the exact accepted product currently shared by both Forge
`dev` refs. `VERSION` remains the version authority and `CHANGELOG.md` remains
the user-facing release chronicle.

The release commit is behavior-neutral. After source, native journey, and
artifact proof pass, create one signed annotated tag locally and publish the
unchanged commit, tag object, and asset matrix independently to both peers.
