## 1. Baseline and authority

- [x] 1.1 Record exact Work Lane HEAD, tree, Lease, active Change, installed AIGW version and digest, configuration schema, selected backend, client discovery, Routes, local branches, both Forge refs, proposals, tags, Releases, CI results, and owned residue.
- [x] 1.2 Preserve the current healthy installation, user configuration, Tokens, Codex and Claude projections, and any selected external endpoint as an immutable rollback baseline.
- [x] 1.3 Map every tracked file and generated projection to one semantic owner, consumer, source of truth, reason to change, dependency direction, and retirement condition.
- [x] 1.4 Map every public command, option, result, schema field, environment variable, native resource, release artifact, documentation entrypoint, and supported user journey to one product invariant and implementation owner.
- [x] 1.5 Reconcile source, tests, canonical specs, this Change, documentation, quality policy, CUE, Forge projections, release metadata, and installed behavior; add every contradiction to this task list before implementation expands.

## 2. Natural setup and activation journey

- [x] 2.1 Add RED acceptance for importing the reviewed team manifest with no Tokens and no clients; require successful capability import and precise deferred actions.
- [x] 2.2 Prove through the setup Store contract that exactly one available Account Token is sufficient, and compose that evidence with each supported backend's conformance and native-platform proof; require unrelated Accounts to remain optional without making setup tests touch host credentials.
- [x] 2.3 Add RED acceptance for one installed client, both installed clients, and neither client; require activation only where Token, Route, and client capability intersect.
- [x] 2.4 Remove any setup validation, prompt, or loop that treats every manifest Account or both clients as mandatory.
- [x] 2.5 Make setup human and JSON results distinguish imported Accounts and Profiles, connected Accounts, selected Routes, projected clients, and deferred continuations.
- [x] 2.6 Prove setup failure before commit leaves configuration, credential slots, client files, and checkpoints byte-identical.
- [x] 2.7 Prove first-time setup commit failure compensates every AIGW-owned write and retains no temporary file, held mutation lock, partial credential, or projection; prove client-owned credentials remain byte-identical, stale client/Account/endpoint projections cannot read Tokens, and public setup failure preserves pre-existing state.

## 3. Route, synchronization, and readiness semantics

- [x] 3.1 Add RED acceptance for separate `aigw use` operations selecting Codex and Claude, followed directly by a successful `aigw check` without a bulk or global selection.
- [x] 3.2 Make `use` update exactly the selected Profile's declared client Route and projection; prove the other client remains byte-identical. Real-file acceptance now covers external conflicts in the unselected client in both directions; selection updates its own projection without planning or rewriting the unrelated one.
- [x] 3.3 Make repeated `use` of the active Profile a no-op when its required Token is already available; report newly acquired Token storage separately without rewriting unchanged configuration, checkpoints, or client files.
- [x] 3.4 Make `sync` discover a client installed after setup and activate its existing eligible Route without repeating setup.
- [x] 3.5 Make `sync` observe a Token supplied after setup and activate only compatible selected or recommended Routes without changing independent user selections.
- [x] 3.6 Remove hidden default, aggregate selection, `--all`, cross-client fallback, and duplicated route-resolution residue from runtime, migration, tests, and docs.
- [x] 3.7 Unify `status`, `check`, and `doctor` classification without overstating evidence: `endpoint_configured` records an address, `check_passed` records applicable command checks, and `endpoint_checked` records successful endpoint diagnostics. Client-native projection success remains `configured`; human and JSON contracts distinguish both from inference and real-client proof, and `next_action` replaces the duplicate `fix` projection.
- [x] 3.8 Replace journal- or implementation-shaped recovery errors with the failed user boundary, current state, and one safe next action.
- [x] 3.9 Prove read-only commands do not mutate, start clients, open credential prompts, or read secret values except for an explicitly declared authenticated probe.

## 4. Credential portability and safety

- [x] 4.1 Define one inspectable backend-selection result containing backend kind, availability, mutability, persistence, and recovery action without secret material. Persistence now comes from current selection metadata rather than a duplicate cached flag; regressions cover external creation, deletion, conflicting choice and invalid metadata without backend reselection or credential-value access.
- [x] 4.2 Prove automatic native backend selection and value-free observation on macOS Keychain without an access prompt.
- [x] 4.3 Prove Linux Secret Service selection on a real user bus and deterministic secure-file fallback when the bus is unavailable.
- [x] 4.4 Prove Windows Credential Manager selection, metadata observation, persistence, replacement, and deletion from a native Windows build.
- [x] 4.5 Prove explicit environment mode across setup, sync, check, credential helpers, and live verification; require stable operation when no native keyring exists.
- [x] 4.6 Ensure API Tokens and Provider diagnostic credentials use distinct typed slots in one selected backend and never cross-read or substitute values.
- [x] 4.7 Remove backend fallback searches, legacy key names, duplicate state markers, stale credential files, and damaged owned entries after migration evidence identifies no consumer.
- [x] 4.8 Prove interruption and failure compensate owned credential and backend-selection state, including `use` cancellation and failed convergence; preserve newer credentials, report incomplete compensation, and keep committed credentials when result rendering fails.

## 5. Client projection and product boundary

- [x] 5.1 Derive one admitted-client contract for discovery, configuration planning, credential-helper projection, atomic projection, status, verification, rollback, and uninstall.
- [x] 5.2 Make Codex CLI and Desktop use the same discovered Codex Home while preserving Desktop-only settings, sessions, models chosen by existing conversations, JSONL, and SQLite.
- [x] 5.3 Make Claude Code projection use its official settings and credential-helper surfaces without a shell wrapper, PATH dependency, profile mutation, or plaintext Token.
- [x] 5.4 Prove missing Codex, missing Claude, later installation, changed executable path, multiple Codex homes, malformed user configuration, and foreign managed-state conflicts.
- [x] 5.5 Prove client projection preparation is complete before writes and multi-target failure compensates only unchanged AIGW-owned postimages.
- [x] 5.6 Make disable and uninstall remove only AIGW-owned blocks, sidecars, helpers, checkpoints, and command projections while preserving all neighboring user state.
- [x] 5.7 Prove direct HTTPS and an explicit loopback compatibility endpoint are ordinary interchangeable Account choices.
- [x] 5.8 Delete every external-gateway product name, fixed port, install path, lifecycle action, or implicit dependency from AIGW source, team manifests, tests, and docs; retain only implementation-neutral composition semantics.
- [x] 5.9 Close the Claude model-drift incident across source and installed behavior. Source `ed75629` passes full race/coverage and quality proof; review `aaa3a7b` is identical on both peers and passes GitHub native macOS, Linux and Windows. Ownership hashes decoded string values, distinguishes model-only edits from connection or credential conflicts, preserves user preferences on withdrawal, and rejects projection conflicts before persistence. The signed `rc.111` archive passes actual `rc.110` upgrade, rollback, re-upgrade and uninstall acceptance; real Claude Code and Codex complete isolated authenticated SSE journeys. That exact candidate is now installed: program SHA-256 `69a9452e6b27774e94ebcf59417bd8fc1ca091b2db09c7e406bcc663749ce0d0`; immediate predecessor retained. Real-host repeated selection, sync preview and repair preview converge; both exact projected helper commands pass with synthetic environment credentials. Default keyring metadata is available and both Routes are configured. Original AIGW configuration, user preferences, native credential backend and Codex authentication absence are preserved; no client or external service was restarted. Source-bound evidence is under `build/verification/ed756298241eafec726b64fb02a5daef7c96c196/release/`. This closes the local synchronization incident, not live Provider inference, formal release publication, all-platform real-client acceptance or the wider Change.

## 6. Provider and future-client extension

- [x] 6.1 Define the minimal Provider declaration for endpoint protocols, Profiles, models, explicit authentication ownership, and optional diagnostic capability.
- [x] 6.2 Add a synthetic ordinary Provider using data and shared conformance fixtures only; prove no core branch, command, installer case, or policy edit is required.
- [x] 6.3 Model AWS Bedrock or another signing-based service through client-native authentication when the admitted client already owns its credential chain and signer; prove AIGW adds neither credential storage nor a signing Adapter.
- [x] 6.4 Define the client Adapter contract and conformance suite for future Hermes, OpenCode, Pi, Qoder, and other clients without coupling them to Provider policy.
- [x] 6.5 Prove one representative future-client fixture can be admitted without changing existing Codex, Claude, Provider, or external-gateway behavior.
- [x] 6.6 Evaluate maintained libraries and frameworks for configuration, secrets, rendering, update, packaging, protocol, and testing against measured deletion, dependency, security, and maintenance value.
- [x] 6.7 Adopt only candidates with positive net value and delete the superseded custom mechanics in the same closure; record concise decisions for retained custom product semantics.

## 7. Semantic and physical topology

- [x] 7.1 Derive a terminal package and repository map from the product ontology and one-way dependency rules before moving files.
- [ ] 7.2 Review the complete physical topology of source, tests, tools, root files, configuration, documentation, manifests, OpenSpec and generated CI by responsibility, dependencies, consumers and lifetime. Existing domain owners now hold provider diagnostics, process drains, native artifact journeys, VERSION parsing and publication transport. Construction separates native acceptance and evidence without new functions or packages. Source-size remediation preserves complete declarations: CI checks, execution, OpenSpec and native acceptance have named carriers; client recovery, onboarding, credentials, attribution, backup/checkpoint, coverage observation and Forge provenance tests follow those responsibilities. Public Codex tests retain their external package, and platform tags are unchanged. The full repository-wide semantic review remains open; passing a file budget does not prove cohesive organization.
- [x] 7.3 Complete the behavioral ownership review of CLI acceptance, credentials, client projection and upgrade tests. CLI carriers are operation-named; guided/explicit setup and failure cases share their journey, while manifest setup stays distinct. Codex public tests remain external; private projection, inspection, scheduling and transaction tests have precise owners. Platform constraints are retained. Shared real-store and process fixtures replace duplicated implementations; scenario-only fixtures stay with their consumers, without an exported support package or new runner. Source `e31e502` passes exact-HEAD full proof after removing five Store/Runner doubles and duplicate archive/command helpers. The final topology relocation preserves all 497 CLI/Codex/credential test functions, bodies and platform constraints; one name is corrected because it falsely claimed environment credentials while using memory storage. See the test-ownership rationale in `design.md`; native release acceptance and global module-depth review remain separate tasks.
- [x] 7.4 Enforce dependency direction so product packages cannot import repository tooling, tests, Forge code, ETHOS internals, or another client's implementation.
- [ ] 7.5 Remove duplicate constants, schemas, renderers, validators, state models, fixtures, wrappers, aliases, re-exports, unused dependencies, empty directories, and unreachable code; complete the global consumer review before restoring this checkmark. Native duplicate and unused-parameter analysis removed test-only production forwarding, unused inputs and redundant fixtures while retaining explicit artifact identities and platform-dependent behavior. Architecture membership validation now shares one portable-name and uniqueness owner while retaining each declaration’s cardinality; duplicated loops and test setup are removed. A six-target native Go import inventory also removes eleven unused production dependency allowances, including the inapplicable test-only upgrade acceptance owner. Help presentation now uses one grouped rendering path and Cobra ordering; projection tests replace repeated product-decoder reads with independent native JSON observations, and a dropped-helper mutation proves the stronger complete-document assertion. Release construction now shares its existing archive-stage fixture across native paths and removes three duplicate setup blocks; fixture output is limited to actual GoReleaser archives rather than precreating later-step evidence. Current ownership and removals are described in [the topology design](design.md#semantic-topology-precedes-physical-movement); the global consumer audit remains open.
- [x] 7.6 Complete the root and `.config` ownership review. Tracked root and `.config` files have an intentional consumer and placement under [the authority map](../../../docs/governance/change-and-release-policy.md#authority-map). Native discovery stays at root; `.config` carries policy data, implementation stays in tools, and ETHOS owns generic hooks and coordination. The defaults-only Prettier file and seven redundant Taplo options are removed without changing formatter output; `.cbmignore` is corrected to development-tool ownership. Real TOML conformance preserves authored key order, array order and multiline grouping. Full rule effectiveness, hosted hooks, supply freshness and whole-repository topology remain under their own open tasks.
- [x] 7.7 Prove tracked entity count and duplicate ownership decrease while every required invariant retains one reachable implementation and test.
- [x] 7.8 Complete the module-depth review across synchronization, setup, client projection and verification, credentials, upgrade, and repository tooling. The [topology design](design.md#semantic-topology-precedes-physical-movement) records one owner for each retained invariant and recovery boundary. Removed forwarding and caller-coordinated recovery are covered by affected-client, credential, projection, workspace and signed-Git regressions. The final shared-writer correction retains write/close/cleanup causes and closes each handle exactly once. Full repository race and quality gates pass; complete physical topology, quantitative quality, hosted integration and final native product journeys remain separate acceptance tasks.

## 8. Quality authority

- [ ] 8.1 Enforce unique carrier responsibility and effective native-check coverage without a duplicate check inventory. The shared checkout-bound Git inventory preserves tracked ignored files and isolates inherited alternate indexes. Go and release schemas, Markdown rules, Prettier formatting and Mermaid grammar run through their native owners with missing-input and source-preservation conformance. Formatting uses locked defaults and the exact authored inventory, not CLI globs; only official OpenSpec archive history is excluded. Empty supported scopes fail. Native stream consumers prevent partial-pipe failures across all three Node adapters. Markdown rejects local overrides and warnings. Vet checks nilness and unused writes; errcheck rejects unchecked type assertions throughout product, tools and tests. Guarded counterparts pass real cross-checkout checks. Discarded-error review reproduces false success after broken gate-progress or publication output. The shared executor now stops before the unreportable gate; both Forge commands propagate report errors while identifying already-verified publication, without rollback or replay. Real closed-output tests preserve both operation boundaries. OpenSpec and all Forge command result paths now propagate closed-output failures. Eight failing scenarios become passing regressions; signed local peer fixtures preserve published branch/tag objects after report failure. Both complete owner packages pass race tests and three-platform lint; the seven Forge paths also execute as an unprivileged Linux user. Native Windows execution and complete concern coverage remain open.
- [x] 8.2 Consolidate Go format, vet, static analysis, modernization, naming, documentation, error, logging, security, complexity, import, and architecture policy into comprehensible owners.
- [x] 8.3 Adopt mature tools for dead code, dependency hygiene, vulnerabilities, secrets, licenses, SBOM, Markdown format and lint, links, TOML, YAML, JSON, CUE, Actions, and GitLab syntax where they provide net value.
- [x] 8.4 Remove custom generic checks superseded by mature tools and retain custom checks only for explicit AIGW product or repository invariants.
- [ ] 8.5 Reassess structural limits by native measurement, protected risk and cohesive examples without exclusions, weaker assertions or count-only extraction. Three-platform trials at `2a7059fe` measure cyclomatic 20 and 15, cognitive 40, span/statements 110/55, nesting four, six parameters and maintainability 25. The maintainability floor is raised to 25 with product/test diagnostic conformance; full three-target lint passes. Cyclomatic 20 produces 40–41 findings and 15 produces 139–140; resolve their semantic value before lowering the global ceiling. Keep the existing 500-line, cyclomatic 25, cognitive 45, 120/60 span/statements, seven-parameter and nesting-five bounds until that review is complete. Retain the 100-token clone threshold: native Windows proves lower values conflate distinct DPAPI operations; previously consolidated test scenarios and preservation assertions remain intact. The [quality policy](../../../docs/governance/change-and-release-policy.md#quality-and-platform-evidence) owns rationale; raw results remain with the source-bound verifier.
- [x] 8.6 Require useful public and repository-tool documentation without ornamental test comments; verify generated command and configuration documentation cannot drift from source.
- [x] 8.7 Make repository-owned warnings fatal across development, test, build, docs, packaging, native artifacts, and CI; remove all current warnings at their owners.
- [ ] 8.8 Enforce one Conventional Commit grammar through local hooks and both Forge integration paths, including generated release commits, without duplicate parsers or historical exception lists. Forge verification now requires complete subject matching, and tag checks reuse strict SemVer instead of a separate regex; signed-object regressions cover both. Complete hook and hosted integration-path conformance remains open.
- [x] 8.9 Run focused format, lint, type, architecture, dependency, security, docs, configuration, and behavior gates on each migrated owner before one final full suite.
- [ ] 8.10 Verify native rules, scopes, exclusions and failure propagation through local commands, hooks and every required CUE event route. Real subprocess conformance covers formatting, native inputs, empty supported scopes, fragmented inventories, typing, nesting and structural boundaries in product and test source. The package script and quality graph share formatting's executor. Deterministic owner-local cleanup seams preserve primary and cleanup causes without Unix permissions or Windows skips. Native-path Markdown assertions and cleanup conformance pass on all three GitHub native platforms at [8dced624](https://github.com/HengYangDS/aigw-cli/actions/runs/34672975105). GitLab's checkout-local Go installation exposed a second scope defect: relative coverage instrumentation included standard-library dependencies. Exact import paths from the existing package inventory now drive both instrumentation and observation, with undeclared counters rejected and no exclusions or weaker floor. [64ad30c7](https://github.com/HengYangDS/aigw-cli/actions/runs/34673518305) passes macOS and Linux on both peers; Windows is pending. The existing native coverage fixture now includes a checkout-local dependency, retaining declaration-only and zero-statement contracts; an overlay restoring the relative instrumentation fails that regression. Signed-ancestry admission, complete hook wiring, final-HEAD native evidence and analyzer blind spots remain open.
- [ ] 8.11 Complete a cross-concern review of actual coverage, dependencies, dead code, security, licenses, SBOM, docs, configuration and release checks; reconcile each with its existing authority, remove duplicate custom mechanics, and reopen any completion claim unsupported by current evidence. Gate declarations now expose verification-output writes and the quality gate's required npm/OSV network access; warm-cache success is not offline proof. OSV release normalization now binds fixed versions to the affected package and release-range type; exact evidence regressions preserve advisories while excluding foreign package fixes and Git commit hashes. Release-source admission now uses native URL and relative-path contracts instead of manual segment scans. Empty authorities and ambiguous repository coordinates fail before updater HTTP/helper access or build tool execution; valid IPv6 origins and namespace punctuation remain admitted. Publication transport review reproduces credential leakage across redirects and direct external GitHub asset links. Metadata and uploads now remain at their selected endpoints; artifact downloads preserve CDN behavior, HTTPS and bounded redirects while withholding credentials outside the API authority and after a foreign hop. A single asset-authority check also validates upload metadata without forbidding the Forge-selected upload host. Real local HTTP/TLS regressions cover both peers, port changes, return-to-origin, caller-policy preservation and source-independent artifact verification. The remaining cross-concern audit is open.

## 9. Development environment and supply chain

- [x] 9.1 Add minimal cross-platform `mise` tasks named `bootstrap`, `check`, `native`, and `release`; each delegates to existing ecosystem owners and no shell wrapper or parallel task runner remains.
- [x] 9.2 Prove a fresh Work Lane reconstructs independent dependencies, build, coverage, and temporary state from committed locks while sharing only content-addressed caches.
- [ ] 9.3 Remove ambient interpreter, user-site, global mise configuration, system package, sibling checkout, and unpinned network discovery from successful local and hosted paths. Go environment isolation and the cache ownership boundary are implemented in [the development design](design.md#existing-ecosystem-tools-remain-the-development-control-plane). Native early mise configuration now excludes parent projects; one CUE boundary selects CI user/system configuration before discovery. Real subprocess conformance exercises all three projected workflow environments from root and nested directories, rejecting parent/global/system contamination without changing foreign files. Bootstrap copies the actual early configuration instead of injecting a test-only ceiling. Complete the remaining ambient-input audit and hosted cache fill/hit observations; package caches never substitute for a lane's mutable environment or proof. Native npm bootstrap now explicitly includes development dependencies. The existing isolated offline reconstruction test first reproduces missing packages under `NODE_ENV=production`, then passes both production mode and `NPM_CONFIG_OMIT=dev` without changing lockfiles or the operator environment. This is a one-flag correction in the shared task, not a new environment manager.
- [x] 9.4 Audit every direct runtime, development, OpenSpec, Go, Node, mise, packaging, documentation, CI Action, and release dependency against current stable releases. The current refresh also rechecks scoped npm overrides: jsdom 30.0.1 remains unadmitted because its advertised whatwg-url 17.1.1 provenance endpoint still returns HTTP 404; the existing documented exception is not represented as latest.
- [x] 9.5 Advance admitted stable dependencies in their existing owners and regenerate Go, npm, and mise locks deterministically. Keep the documented npm provenance hold explicit; build OSV through the native mise Go backend without weakening call analysis.
- [x] 9.6 Requalify the updated supply chain on macOS, Linux, and Windows: [verify every executable and the OSV compiler](https://github.com/HengYangDS/aigw-cli/actions/runs/34694623110), then [resolve each native lock twice without changing source bytes](https://github.com/HengYangDS/aigw-cli/actions/runs/34695175091). The three independently downloaded lock artifacts match the committed lock; this does not assert release or integration completion.
- [ ] 9.7 Configure one dependency-update proposal owner with release-age policy, safe grouping, vulnerability priority, and object-preserving automatic merge; prove a live signed proposal refreshes native locks and CI projections, passes checks, merges once, and creates no competing peer proposal.
- [x] 9.8 Generate and verify SBOM, vulnerability, license, checksum, signature, and provenance outputs without checkout paths, volatile run timestamps, credentials, or installer-local metadata; derive any format-required time from the release identity.
- [x] 9.9 Evaluate the current Go release construction against maintained alternatives only where measurements show meaningful gains; avoid replacing the native Go toolchain with a second packaging framework.

## 10. CI and dual-Forge projection

- [x] 10.1 Define the complete semantic CI graph in CUE: fast quality, Go matrix, native source evidence, artifact construction, lifecycle acceptance, release metadata, publication, and parity.
- [x] 10.2 Generate GitHub Actions and GitLab CI from that model; reject hand-edited drift while preserving only provider syntax and runner-capability deltas.
- [x] 10.3 Cover proposal creation and update, review commit, maintainer fast-forward, direct `dev` push, `main`, and tag events with required exact-commit evidence; close the direct-`dev` trigger gap on both Forges. Review admission now covers both integration and release targets, including `dev` to `main`; GitLab workflow and job admission share the existing CUE rule set. Projection regressions cover both default and renamed branch roles, required native jobs and the post-push parity boundary. Hosted execution and merge acceptance remain under 10.7–10.9.
- [x] 10.4 Separate independently useful jobs and parallelize safe matrices; remove monolithic verification and duplicate jobs that prove no additional fact.
- [x] 10.5 Define exact conditions for evidence reuse and require rerun when source, platform, environment, lock, toolchain, release, or claimed fact changes.
- [x] 10.6 Assign Windows product proof to an available native runner without requiring a symmetric but unavailable GitLab Windows runner.
- [ ] 10.7 Verify GitHub and GitLab authentication, SSH transport, author identity, commit and tag signature, protected branches, unprotected proposal branches, automatic merge, and source-branch deletion without password prompts.
- [ ] 10.8 Repair AIGW GitHub `main` and `dev` lag using the exact locally accepted signed objects; verify both Forges host identical Git object IDs and no Forge rewrites identity.
- [ ] 10.9 Keep one proposal per Change, update its exact commit, require green evidence, merge it by the admitted developer or maintainer path, and delete the proposal ref promptly.

## 11. Documentation and user experience

- [x] 11.1 Review documentation ownership by responsibility and audience: research, adopted decisions, current architecture, concepts, user journeys, operations and contributor policy. Correct mixed-purpose documents and references without adding empty indexes.
- [x] 11.2 Make every directory index purposeful, remove empty or redirect-only README files, and ensure internal concepts, commands, decisions, evidence, and stable external standards use valid descriptive links.
- [x] 11.3 Rewrite setup, team manifest, credential backend, client-later-installation, direct endpoint, optional external-gateway composition, recovery, and uninstall journeys in precise natural language.
- [ ] 11.4 Generate CLI reference from the command surface or verify it against source; audit every command name, default, option, exit code, JSON field, error, and next action for narrow semantics, including `check` endpoint ownership and the distinction between catalogue membership, authenticated inference, and real-client readiness. Codex root selection now uses the locked TOML parser rather than global regex replacement: dollar-sign model IDs, quoted keys, multiline originals and named-profile settings survive projection and withdrawal; invalid models leave no file changes or sidecar residue. Scheduler table edits now share native TOML boundaries, leaving example headers in multiline instructions and neighboring profile keys untouched; independent typed decoding detects the prior shared writer/verifier mistake. Quoted headers and integer spellings round-trip by value without a global comment rewrite. Completed corrections cover evidence-bounded check and doctor outcomes, recovery execution independent of output format, preservation of committed state and write failures, one root error result, and credential-only helper stdout. Command help now projects native Cobra descriptions and examples plus pflag types, declared defaults and inherited options; the complete command-tree test verifies option coverage and no configuration writes. Account finalization shape and manifest Token ownership now fail at argument admission, before configuration locking or manifest reads; command help supplies the exact recovery action. Native Cobra mutually exclusive flag pairs replace setup's custom conflict scan, retaining direct-mode flag combinations. Argument admission now precedes configuration locking: native flag relationships and update, setup, selection, verification, adapter, creation and import shapes reject invalid invocations with actionable guidance, without creating configuration storage or invoking operations, credentials or client discovery. Synchronization owns service creation, sharing selection's projection and credential compensation while preserving unrelated clients. Regressions cover identity admission before Token acquisition, actual storage and recovery, and pre-operation lock failures. Verification target admission also precedes configuration reads: missing or empty targets and unsupported clients report argument errors even when configuration is corrupt; Cobra owns mutually exclusive target flags, including explicit empty values. Endpoint testing likewise rejects explicit empty selectors before configuration or credential access rather than broadening them to all selected Routes; omission retains the documented selected-route default. Account and Profile rename now share positional admission before configuration locks: missing noninteractive identifiers, malformed explicit identifiers and unavailable interactive prompts fail with command-specific help. Finalization reuses the same identifier contract; complete explicit requests need no prompt and guided rename remains supported. The selection stage no longer repeats late argument checks. The remaining command behavior audit is still open.
- [ ] 11.5 Normalize semantic organization and presentation across code, schemas, manifests, docs, help, and generated projections; review every current Markdown diagram, table, code block, list, heading, link, and paragraph for truthful content, logical hierarchy, consistent terminology and readable layout; enforce one team label, purpose, recommendation, and native-export authoring contract.
- [ ] 11.6 Deliver one reviewed directly consumable team profile with current provider identifier evidence, authenticated protocol and real-client acceptance for recommendations, no credentials in version control, and safe local overrides; distinguish configured coverage from verified availability. Complete released-AIGW setup/sync, supported-platform acceptance and durable publication beyond the existing 34-Profile macOS inference evidence.
- [ ] 11.7 Remove stale claims, contradictory instructions, historical workarounds, compatibility notes without consumers, and evidence directories that do not own durable product facts; retain policy only under its actual audience owner and verify claims against their sources. The fully superseded GitHub-only decision is deleted after checking its successor and inbound links. Control-plane guidance no longer implies a mandatory proxy; credential guidance now distinguishes initial metadata observation, persisted choice, operation permission and client-process environment inheritance. The remaining whole-document audit stays open. The quality policy now retains current acceptance criteria, metric semantics, trade-offs and the evidence ladder while deleting old trial counts, obsolete performance measurements and repeated implementation diaries. Its former 431-line undivided quality section is organized into six responsibility-based subsections. Formatting and diagram scope belong to the existing text policy; its obsolete Prettier CLI/configuration explanation is corrected to the actual checkout-bound API adapter. The two documents shrink by 239 lines without changing machine thresholds or adding a carrier. Native format, Markdown, local-link and architecture checks pass. Local browser measurements at 1440 and 390 pixels show no page overflow; screenshots are retained, but complete visual and hosted rendering acceptance remain open.
- [ ] 11.8 Verify explicit links and actual Markdown rendering, including Mermaid semantics and syntax, table readability, code-block language and copyability, list nesting, heading navigation, and accessibility; also prove terminal layouts, narrow consoles, JSON stability, executable documented journeys and fresh-user usability. Native Markdown conformance now checks semantic headings, permitted OpenSpec labels and table alignment. Five changed documents render at wide and narrow sizes with no page-level overflow and accessible Mermaid metadata; the local previews do not establish hosted rendering or complete visual acceptance. Lint or formatting success alone does not establish rendered or semantic acceptance.

## 12. Cross-platform product and release evidence

- [ ] 12.1 Prove clean-room source bootstrap and the full fast gate on macOS, Linux, and Windows from the exact candidate commit.
- [ ] 12.2 Build immutable native artifacts for every declared OS and architecture; verify embedded version, checksum, signature, SBOM, provenance, startup, help and absence of checkout dependencies. Signed source `9d577238` produces twelve byte-identical files across two macOS arm64 builds: six archives, SPDX SBOM, license/vulnerability reports, provenance, checksums and detached signature. Both matrices pass native validation and independent byte comparison; checksum manifest SHA-256 is `3612d4b9f43196d1a18c86474bef56c725c071908408da2b0afdd0d38bbf360b`. OpenSSH verifies the `aigw-release` signature against an independently selected key. Evidence is under `build/verification/9d577238843f1d9873f7196b6843b4d121075e8c/packaged-candidate/`. The distinct `rc.112` successor at source `4bfcc033` also produces two identical twelve-file matrices, with checksum manifest SHA-256 `a92c6843877dd3864fb6ca2e9e0c943a6490435085da55c88be6abb7c1507b6e` and independently verified SSH signature. Evidence is under `build/verification/4bfcc0334c7c4dced0798ae66b84bb33433aac91/successor-artifact/`. This proves same-host reproducibility, not cross-host reproducibility, final tag identity or publication. Build inputs select GitHub only; no peer is inferred from SSH transport and no personal key is uploaded.
- [x] 12.3 Prove fresh install, status, setup, use, sync, check, doctor, update, rollback, recovery, disable and uninstall on macOS. The signed candidate passes packaged upgrade/rollback and real Codex/Claude journeys; a separate isolated 17-command matrix proves client-scoped selection, repeated-use byte preservation, configuration rollback and repeated-repair terminality. [Native hosted qualification](https://github.com/HengYangDS/aigw-cli/actions/runs/34695175091) proves Keychain and published-predecessor lifecycle on an ephemeral host. Operator installation and configuration remain byte-identical. Formal publication and installed-product replacement remain under 13.6.
- [x] 12.4 Prove the supported product journey on native Linux, including Secret Service and headless fallback. The current Linux arm64 program passes delayed client/Token activation, one connected Account, packaged upgrade/rollback/uninstall, native Secret Service continuity and no-session-bus file fallback as an unprivileged user. Native [GitHub qualification](https://github.com/HengYangDS/aigw-cli/actions/runs/34695175091) and [GitLab qualification](http://192.168.64.101:18086/dig/misc/tools/llm-third-party-api/aigw-cli/-/pipelines/6502) also pass. Exact program bytes, container isolation and cleanup are recorded with the source-bound native evidence. Live Provider inference, final Release assets and installed-runtime claims remain separate obligations. The documented upstream GNOME Keyring replacement warning is not suppressed or represented as an AIGW credential duplication.
- [x] 12.5 Prove the supported product journey on native Windows, including Credential Manager, path conventions, process environment, recoverable program replacement and exact cleanup. Source `9dec1b6d` passes [native Windows acceptance](https://github.com/HengYangDS/aigw-cli/actions/runs/34694623110) and [historical artifact and real-client qualification](https://github.com/HengYangDS/aigw-cli/actions/runs/34695175091). The native suite covers UNC and extended-length derivation plus transient and persistent source/destination handle locks; the packaged journey preserves credentials and user files through upgrade, rollback and uninstall. Final published-asset identity remains a separate release obligation in 12.2 and 13.6.
- [ ] 12.6 Prove real Codex CLI and Claude Code projections from the released artifact, including installation after setup, invocation outside the installer shell, reasoning-effort settings and supported context limits. The signed `9d577238` macOS archive passes real Codex 0.154.0 authenticated SSE completion at published predecessor, candidate, rollback and re-upgrade, plus `aigw verify --for codex`. Claude Code 2.1.236 passes setup before client installation, one connected Account among two, later discovery/sync, projected-helper authentication, SSE completion, disable and uninstall. Temporary homes and synthetic environment credentials isolate the operator. Codex auth/history preservation is relative to published-predecessor setup; Claude discovery HEAD is distinct from authenticated inference. Driver digests, client hashes and raw logs reside under 12.2. These controlled-upstream results do not prove live Provider inference. The same signed Linux arm64 archive also passes real Claude Code 2.1.236 twice as UID 1000: environment credentials without a session bus, and native Secret Service with no Token environment variable. Both run setup before client installation, later sync, credential-helper authentication and SSE completion, user-setting preservation, disable and uninstall. The Claude binary matches its official native manifest; the driver Node binary matches the repository lock. Containers have no network or host mounts and are removed with their synthetic credentials and temporary files. Evidence is under `build/verification/021a5f539b2a6626a130a2daaf637757f1f8533d/linux-real-claude/readable-inputs/verified-journeys.json`. Real Linux Codex 0.154.0 also completes all four phases against the same signed artifact: published `rc.110`, candidate, rollback and re-upgrade, followed by uninstall. Environment and native Secret Service credentials each pass 21 commands, authenticated SSE, `aigw verify` and byte-exact post-baseline auth/history preservation. The complete official Codex package, bubblewrap and repository-locked Node inputs are hash-verified inside a networkless, mountless container. Native preflight verifies its registered UID 1000, user bus and sandbox namespaces; clients execute without capabilities or privilege escalation. Both final journey logs contain no client warnings or errors. Evidence is under `build/verification/7366d38279f8c28b9fb3e45257669ecbac3464e9/linux-real-codex/native-user-vault/result.json`. The tracked `TestNativeClientJourney` now reuses native release fixtures for both clients and all four lifecycle phases; macOS passes with the same signed archive, Codex 0.154.0 and Claude Code 2.1.236. Input and SSE contract regressions execute in the shared quality sequence, tagged code is always linted, and missing client/artifact inputs fail rather than skip. The fixture environment update now preserves explicit credentials while initial setup still removes ambient Tokens. The release owner now exposes `accept-native --clients` to reuse one built candidate for lifecycle and real-client tests with ordered failure propagation and teardown. CUE projects an explicit `windows_clients` qualification into the existing manual historical job; missing predecessor input fails, pinned native packages are signature-checked, install scripts stay disabled, and temporary clients are removed. Native Windows job 103524522009 now passes this path with real Codex 0.154.0 and Claude Code 2.1.269: published predecessor, candidate, rollback, re-upgrade and uninstall, authenticated controlled-upstream SSE and user-file preservation. Three npm packages have verified registry signatures and two have verified attestations. This tracked client journey uses environment credentials; real Credential Manager passes separately in the native product journey. Actual installed `rc.111` exposed an acceptance assumption: unlike `rc.110`, it correctly leaves Codex authentication storage absent. The tracked observer now preserves both presence and exact bytes without rewriting `auth.json`; focused conformance covers absent, empty, existing, changed, deleted and wrong-kind storage and runs through the shared quality command. Reasoning/context settings, live Provider inference and final published identity remain open. These exact Linux product bytes come from `9d577238`, not a newly published release.
- [ ] 12.7 Prove direct Provider endpoints and an explicitly selected external compatibility endpoint without giving AIGW ownership of the external service.
- [ ] 12.8 Prove native update from the actual retained published predecessor archive, failed-successor rollback, repeated recovery terminality and exact uninstall on every supported platform; verify digests, credential continuity and all unowned state. Signed `9d577238` macOS and Linux arm64 archives pass against independently digest-verified published `rc.110`. macOS additionally completes real Codex requests through rollback and re-upgrade; Linux proves native Secret Service continuity. [Explicit hosted qualification](https://github.com/HengYangDS/aigw-cli/actions/runs/34676661552) passes historical lifecycle on macOS and Linux; macOS also passes Keychain continuity. Windows job 103507613653 also passes native product, system credential-store and historical lifecycle acceptance against digest-verified `rc.110`; the saved job and raw log are under `build/verification/9d577238843f1d9873f7196b6843b4d121075e8c/qualification/`. The run's historical-ancestry quality gate still fails; native success does not make the whole run green. Linux Codex now completes both credential-backed rollback and re-upgrade journeys in 12.6. Windows job 103524522009 additionally proves real Codex and Claude rollback and re-upgrade against its source-bound candidate in 12.6. Final published identity and Linux Claude rollback remain open; no seamless rollback is claimed. Repeated portable installation now preserves an existing predecessor or its absence and keeps identical program bytes in place. The previous signed rc.112 artifact reproduces predecessor loss; the four-line owner correction passes the final regression against old/new implementations, native macOS CLI inode-and-byte checks, the Linux arm64 installation suite, sixteen CLI package race tests, and three-OS lint. Windows compilation is preflight only; native execution and final publication remain open. Evidence is under `build/verification/f692767bc0122d747e50bbb6027c7ef8e23578a5/install-idempotence/`.
- [ ] 12.9 Compare performance, memory, startup, binary size, projection latency, and credential access against explicit budgets and the retained baseline. Source `d077e69` macOS arm64 measurement with Hyperfine 1.20.0, five warmups and 80 shell-free runs: candidate/baseline medians are 3.761/3.670 ms for `--version` and 3.661/3.601 ms for `--help`; binaries are 10,715,810/10,580,770 bytes. Raw measurements reside under the source-bound verification output. Other platforms, memory, projection and credential measurements remain open.

## 13. Destructive cleanup and terminal closeout

- [ ] 13.1 Re-run the semantic inventory and delete every obsolete implementation, migration path, schema reader, wrapper, alias, generated file, cache, runtime, temporary service, branch, tag, Release, or record with no current consumer.
- [ ] 13.2 Verify `.gitignore`, attributes, editor settings, root metadata, `.config`, OpenSpec, docs, tests, tools, and package layout contain only current intentional carriers in stable logical order; keep `.serena`, `.code-memory`, and `.codebase-memory` host-local where present.
- [ ] 13.3 Verify every user feedback item in this Change maps once to an implemented invariant and exact evidence; disclose every remaining external unknown without duplicating work.
- [ ] 13.4 Run all format, lint, type, architecture, dependency, security, documentation, configuration, behavior, native, release, and installed-runtime gates from a pristine candidate.
- [ ] 13.5 Verify signed clean candidate HEAD, exact tree, complete OpenSpec progress, current ETHOS proof, no P0 or P1 defect, and no warning or hidden skipped required gate.
- [ ] 13.6 Complete successor-version acceptance and publication. `VERSION` now reserves `0.1.0-rc.112`; local and both peer tag namespaces had no matching tag at allocation. The actual installed `rc.111` program matches retained source `ed756298` byte-for-byte, SHA-256 `69a9452e6b27774e94ebcf59417bd8fc1ca091b2db09c7e406bcc663749ce0d0`, and remains untouched. An isolated copy of those actual installed bytes now passes packaged upgrade, rollback, re-upgrade and uninstall against the signed `4bfcc033` successor artifact; real Codex 0.154.0 and Claude Code 2.1.236 pass authenticated controlled-upstream SSE at all four phases. The verifier correction preserves absent Codex authentication storage and is independently regression-tested. The artifact remains uninstalled and unpublished. Archive this Change through current ETHOS so canonical specs lose superseded route semantics, land the signed candidate, align local and both Forge `main` and `dev`, publish and verify the signed tag and identical release assets, and confirm all required CI is green.
- [ ] 13.7 Delete the merged proposal, retire the Work Lane, prune obsolete local and remote refs and owned runtime residue, and verify the repository family and installed product are clean and healthy.
