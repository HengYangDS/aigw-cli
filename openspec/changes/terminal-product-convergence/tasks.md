## 1. Preserve completed fixes

- [x] 1.1 Fix GitHub trust-input materialization and host-independent policy
  paths with regression-first tests.
- [x] 1.2 Restore the shared native Codex Home and prove absent native clients
  are untouched.
- [x] 1.3 Upgrade the Go module graph and immutable CI inputs to current stable
  supported releases.
- [x] 1.4 Separate AIGW, native Codex, Claude Code, Proxy, foreign applications,
  Workstation, and Forge authority without cross-product verification.

## 2. Reconstruct the product kernel

- [ ] 2.1 Prove an ordinary provider requires only token-free Account/Profile
  data and no provider-specific command, projection branch, installer case, or
  core dependency.
- [ ] 2.2 Remove the Codex storage/Azure identity workaround; retain only an
  explicit projection capability supported by the admitted Codex contract.
- [ ] 2.3 Decouple provider-native diagnostics from ordinary configuration,
  routing, projection, and endpoint verification.
- [ ] 2.4 Make Codex and Claude Code cohesive adapters and replace cross-cutting
  client-name switches with one admitted registry and bounded interfaces; prove
  a synthetic future client has a local change radius without implementing it.
- [ ] 2.5 Prove setup and repair configure only installed clients and never
  create, launch, or mutate absent or foreign clients.

## 3. Converge architecture, UX, and DX

- [ ] 3.1 Absorb required semantic-housekeeping and lane deltas under terminal
  owners; remove flat, forwarding, alias, re-export, compatibility, duplicated,
  stale, and misplaced residue.
- [ ] 3.2 Audit source, tests, scripts, CI, packaging, examples, team manifests,
  docs, comments, and docstrings for current truth, portability, and hard-coding.
- [ ] 3.3 Keep daily UX task-oriented and quiet; failures SHALL not emit an
  unexpected traceback, warning, or usage banner.
- [ ] 3.4 Establish repository-owned verification and package plus aggregate
  coverage strictly above 95%, with native macOS/Linux/Windows evidence.

## 4. Prove, publish, accept, and close

- [ ] 4.1 Run focused tests, full local gates, strict OpenSpec validation, and a
  pristine exact-HEAD proof.
- [ ] 4.2 Commit with the GitLab publication actor and trusted signature, land,
  replay the content for GitHub, and publish both Forge planes with exact-tip
  green CI, compare-and-swap controls, signed tags/releases, and asset parity.
- [ ] 4.3 Install only verified release assets and execute UCloud, DMXAPI, and
  AIHubMix routes; native Codex through the AIGW-projected Proxy endpoint;
  native Claude Code; Proxy replay/empty-response/429/provider-portability; and
  exact original Codex conversation acceptance independently.
- [ ] 4.4 Run owner-bound closeout for every lane, worktree, branch, lease,
  temporary checkout, old service, and generated residue; finish with clean
  canonical roots and freshly verified publication facts.
