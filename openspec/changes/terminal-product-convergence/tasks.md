## 1. Baseline and authority

- [x] 1.1 Record exact Work Lane HEAD, tree, Lease, active Change, installed AIGW version and digest, configuration schema, selected backend, client discovery, Routes, local branches, both Forge refs, proposals, tags, Releases, CI results, and owned residue.
- [x] 1.2 Preserve the current healthy installation, user configuration, Tokens, Codex and Claude projections, and any selected external endpoint as an immutable rollback baseline.
- [x] 1.3 Map every tracked file and generated projection to one semantic owner, consumer, source of truth, reason to change, dependency direction, and retirement condition.
- [x] 1.4 Map every public command, option, result, schema field, environment variable, native resource, release artifact, documentation entrypoint, and supported user journey to one product invariant and implementation owner.
- [x] 1.5 Reconcile source, tests, canonical specs, this Change, documentation, quality policy, CUE, Forge projections, release metadata, and installed behavior; add every contradiction to this task list before implementation expands.

## 2. Natural setup and activation journey

- [x] 2.1 Add RED acceptance for importing the reviewed team manifest with no Tokens and no clients; require successful capability import and precise deferred actions.
- [x] 2.2 Prove through the setup Store contract that exactly one available Account Token is sufficient, and compose that evidence with each supported backend's conformance and native-platform proof; require unrelated Accounts to remain optional without making setup tests touch host credentials.
- [x] 2.3 Add RED acceptance for one installed client, both installed clients, and neither client; require activation only where Token, Route, and client capability intersect.
- [x] 2.4 Remove any setup validation, prompt, or loop that treats every manifest Account or both clients as mandatory.
- [x] 2.5 Make setup human and JSON results distinguish imported Accounts and Profiles, connected Accounts, selected Routes, projected clients, and deferred continuations.
- [x] 2.6 Prove setup failure before commit leaves configuration, credential slots, client files, and checkpoints byte-identical.
- [x] 2.7 Prove first-time setup commit failure compensates every AIGW-owned write and retains no temporary file, held mutation lock, partial credential, or projection; prove client-owned credentials remain byte-identical, stale client/Account/endpoint projections cannot read Tokens, and public setup failure preserves pre-existing state.

## 3. Route, synchronization, and readiness semantics

- [x] 3.1 Add RED acceptance for separate `aigw use` operations selecting Codex and Claude, followed directly by a successful `aigw check` without a bulk or global selection.
- [x] 3.2 Make `use` update exactly the selected Profile's declared client Route and projection; prove the other client remains byte-identical. Real-file acceptance now covers external conflicts in the unselected client in both directions; selection updates its own projection without planning or rewriting the unrelated one.
- [x] 3.3 Make repeated `use` of the active Profile a no-op when its required Token is already available; report newly acquired Token storage separately without rewriting unchanged configuration, checkpoints, or client files.
- [x] 3.4 Make `sync` discover a client installed after setup and activate its existing eligible Route without repeating setup.
- [x] 3.5 Separate imported recommendations from actual selections at the configuration owner. Setup and sync fill only unselected clients, prefer an available recommendation and its model, and preserve independent choices and client-native authentication. The actual team manifest passes no-Token and late AIHubMix/DMXAPI/UCloud activation, credential-helper execution, repeated-sync byte stability and uninstall. Affected configuration, client, synchronization, CLI and rename packages pass race tests; full source passes at 97.19% (9899/10185 statements). Import directs one connected Account to sync rather than demanding every Token. Evidence: `build/verification/a5c18a8b2db951dfea13fa03f49dbda4b941c7bc/` (`team-manifest-green.log`, `route-consumers-final.log`, `route-source-final.log`). Current-host source behavior is proved; hosted execution and predecessor compatibility with newly generated recommendations remain delivery obligations in 12.6 and 12.8.
- [x] 3.6 Remove hidden default, aggregate selection, `--all`, cross-client fallback, and duplicated route-resolution residue from runtime, migration, tests, and docs.
- [x] 3.7 Unify `status`, `check`, and `doctor` classification without overstating evidence: distinguish configured addresses, local projections, endpoint diagnostics and real-client inference. Enabled projection failures precede credential absence; only executed diagnostics refine state. Account admission and readiness now share standard loopback-host identity. Status shows every selected loopback Route without inferring service identity or health; check retains the external-service boundary. RED/GREEN regressions, affected race suites, three-target lint and full source pass at 97.18% (9935/10223 statements). Four isolated native status runs at 40/120 columns and human/JSON output preserve configuration bytes and clean owned scratch. Evidence: `build/verification/143e25e72001356530f69aa0482f675c77d3b2ec/endpoint-*.log` and `endpoint-native.json`. Final hosted and released-byte evidence remain delivery obligations.
- [ ] 3.8 Replace journal- or implementation-shaped recovery errors with the failed user boundary, current state, and one safe next action. The rc.113 silent helper failure is repaired in released and installed rc.114. The installed executable keeps stdout credential-only, emits safe recovery guidance on stderr, preserves underlying causes and rejects stale projections before secret access. Its exact packaged bytes pass 24 independent isolated checks, full source proof, and published macOS/Linux/Windows lifecycle and real-client acceptance. Installed synthetic-environment probes prove current fingerprint success and stale fingerprint rejection without reading production credentials. The historical incident still lacks its original helper arguments; current DMX HTTP 200 does not prove the old in-memory cause. Actual incident follow-up remains open with the credential owner. Evidence: `build/verification/2946d0aed02ce52dbf9d5ce63b5a047a8f43d7a3/installation/credential-diagnostics.json` and `published-native-terminal.json`.
- [x] 3.9 Prove read-only commands do not mutate, start clients, open credential prompts, or read secret values except for an explicitly declared authenticated probe. Readiness now validates each enabled projection before reading Token values or probing endpoints. Four command regressions reproduce and close premature reads; existing missing-Token tests now establish valid client prerequisites rather than rely on the invalid ordering. The redundant tokenAvailable flag and duplicate status-error construction are removed. Shared classification, all CLI/client/synchronization race tests, three-target lint and full source pass at 97.18% (9930/10218 statements). Source-built check/status/doctor tests agree on configuration recovery; no host credentials, client settings or installed program were changed, and exact scratch is removed. Evidence: `build/verification/b6ba18f630462bf18b73e256d734771510f5ca4d/check-*.log`, `check-credential-native-final.json` and `check-credential-native-cleanup.json`. Hosted current-commit and final released-byte acceptance remain separate.

## 4. Credential portability and safety

- [x] 4.1 Define one inspectable backend-selection result containing backend kind, availability, mutability, persistence, and recovery action without secret material. Persistence now comes from current selection metadata rather than a duplicate cached flag; regressions cover external creation, deletion, conflicting choice and invalid metadata without backend reselection or credential-value access.
- [x] 4.2 Prove automatic native backend selection and value-free observation on macOS Keychain without an access prompt.
- [x] 4.3 Prove Linux Secret Service selection on a real user bus and deterministic secure-file fallback when the bus is unavailable.
- [x] 4.4 Prove Windows Credential Manager selection, metadata observation, persistence, replacement, and deletion from a native Windows build.
- [x] 4.5 Prove explicit environment mode across setup, sync, check, credential helpers, and live verification; require stable operation when no native keyring exists.
- [x] 4.6 Ensure API Tokens and Provider diagnostic credentials use distinct typed slots in one selected backend and never cross-read or substitute values.
- [x] 4.7 Remove backend fallback searches, legacy key names, duplicate state markers, stale credential files, and damaged owned entries after migration evidence identifies no consumer.
- [x] 4.8 Prove interruption and failure compensate owned credential and backend-selection state, including `use` cancellation and failed convergence; preserve newer credentials, report incomplete compensation, and keep committed credentials when result rendering fails.

## 5. Client projection and product boundary

- [x] 5.1 Derive one admitted-client contract for discovery, configuration planning, credential-helper projection, guarded commit and compensation, status, verification, rollback, and uninstall.
- [x] 5.2 Make Codex CLI and Desktop use the same discovered Codex Home while preserving Desktop-only settings, sessions, models chosen by existing conversations, JSONL, and SQLite.
- [x] 5.3 Make Claude Code projection use its official settings and credential-helper surfaces without a shell wrapper, PATH dependency, profile mutation, or plaintext Token. Equivalent JSON escapes and whitespace now preserve readiness and cause no settings or sidecar write: the existing owned-value hash supplies the no-op decision, replacing byte-layout comparison. RED reproduced needless projection; focused regressions preserve both files, while model-only recovery and connection conflicts keep their existing owners. The native historical lifecycle now requires exact user-file bytes rather than comparing unnormalized RawMessage fragments. All affected race tests, lint, size and complete source gates pass (97.15%, 9933/10224 statements); public native team, predecessor and actual Codex/Claude controlled-stream journeys pass. Evidence: `build/verification/683b22880f6c020e15ab9bb7c33155b3a67fbd05/claude-semantic-noop/`. No new file or alternate hash model is introduced. This is source/candidate proof; the operator installation and its genuinely missing Claude model remain unchanged pending safe delivery.
- [x] 5.4 Prove missing clients, delayed installation, executable changes, multiple Codex homes, malformed user configuration and foreign-state conflicts. Native TOML ownership preserves Codex MCP edits without absorbing user-owned tables. The strict provider model retains valid predecessor authentication fields and rejects altered owned values; no alternate parser or version branch remains. Exact signed 054754ea passes ordinary GitHub 34754037525, GitLab 6572 and full historical qualification 34754065802 on macOS, Linux and Windows against published rc.110. Native Codex 0.154.0 and Claude Code 2.1.269 complete baseline, upgrade, rollback and re-upgrade; Windows also passes system credential storage. Source and focused regressions pass; operator configuration remains unchanged. Evidence: `build/verification/054754eaddf1c576f7a3de45b2fa80733284cef6/` (`historical-terminal.json`, `historical-jobs.json`, `historical-windows.log`) and the earlier focused logs referenced by the native authentication regression. Final published-byte and operator-installation acceptance remain under 12.6, 12.8 and 13.6.
- [x] 5.5 Prove client projection preparation is complete before writes and multi-target failure compensates only unchanged AIGW-owned postimages.
- [x] 5.6 Make disable and uninstall remove only AIGW-owned blocks, sidecars, helpers, checkpoints, and command projections while preserving all neighboring user state.
- [x] 5.7 Prove direct HTTPS and an explicit loopback compatibility endpoint are ordinary interchangeable Account choices.
- [x] 5.8 Delete every external-gateway product name, fixed port, install path, lifecycle action, or implicit dependency from AIGW source, team manifests, tests, and docs; retain only implementation-neutral composition semantics.
- [x] 5.9 Close the Claude model-drift incident across source and installed behavior. Source `ed75629` passes full race/coverage and quality proof; review `aaa3a7b` is identical on both peers and passes GitHub native macOS, Linux and Windows. Ownership hashes decoded string values, distinguishes model-only edits from connection or credential conflicts, preserves user preferences on withdrawal, and rejects projection conflicts before persistence. The signed `rc.111` archive passes actual `rc.110` upgrade, rollback, re-upgrade and uninstall acceptance; real Claude Code and Codex complete isolated authenticated SSE journeys. That exact candidate is now installed: program SHA-256 `69a9452e6b27774e94ebcf59417bd8fc1ca091b2db09c7e406bcc663749ce0d0`; immediate predecessor retained. Real-host repeated selection, sync preview and repair preview converge; both exact projected helper commands pass with synthetic environment credentials. Default keyring metadata is available and both Routes are configured. Original AIGW configuration, user preferences, native credential backend and Codex authentication absence are preserved; no client or external service was restarted. Source-bound evidence is under `build/verification/ed756298241eafec726b64fb02a5daef7c96c196/release/`. This closes the local synchronization incident, not live Provider inference, formal release publication, all-platform real-client acceptance or the wider Change.

## 6. Provider and future-client extension

- [x] 6.1 Define the minimal Provider declaration for endpoint protocols, Profiles, models, explicit authentication ownership, and optional diagnostic capability.
- [x] 6.2 Add a synthetic ordinary Provider using data and shared conformance fixtures only; prove no core branch, command, installer case, or policy edit is required.
- [x] 6.3 Model AWS Bedrock or another signing-based service through client-native authentication when the admitted client already owns its credential chain and signer; prove AIGW adds neither credential storage nor a signing Adapter.
- [x] 6.4 Define the client Adapter contract and conformance suite for future Hermes, OpenCode, Pi, Qoder, and other clients without coupling them to Provider policy.
- [x] 6.5 Prove one representative future-client fixture can be admitted without changing existing Codex, Claude, Provider, or external-gateway behavior.
- [x] 6.6 Evaluate maintained libraries and frameworks for configuration, secrets, rendering, update, packaging, protocol, and testing against measured deletion, dependency, security, and maintenance value.
- [x] 6.7 Adopt only candidates with positive net value and delete the superseded custom mechanics in the same closure; record concise decisions for retained custom product semantics.

## 7. Semantic and physical topology

- [x] 7.1 Derive a terminal package and repository map from the product ontology and one-way dependency rules before moving files.
- [x] 7.2 Review the complete physical topology of source, tests, tools, root files, configuration, documentation, manifests, OpenSpec and generated CI by responsibility, dependencies, consumers and lifetime. The [repository-wide ownership review](design.md#repository-wide-ownership-review) accounts for all 1,020 tracked carriers and 59 native Go packages on each of six OS/architecture targets. Native import inventories contain no product-to-tool edges. Private tests and OS selection retain their actual boundaries; documents, configuration and generated outputs have explicit owners and lifetimes. Generic lifecycle overlap, global deletion, effective quality, rendered UX and final artifacts remain separate open tasks. The design removes superseded repair history instead of introducing another inventory document.
- [x] 7.3 Complete the behavioral ownership review of CLI acceptance, credentials, client projection and upgrade tests. CLI carriers are operation-named; guided/explicit setup and failure cases share their journey, while manifest setup stays distinct. Codex public tests remain external; private projection, inspection, scheduling and transaction tests have precise owners. Platform constraints are retained. Shared real-store and process fixtures replace duplicated implementations; scenario-only fixtures stay with their consumers, without an exported support package or new runner. Source `e31e502` passes exact-HEAD full proof after removing five Store/Runner doubles and duplicate archive/command helpers. The final topology relocation preserves all 497 CLI/Codex/credential test functions, bodies and platform constraints; one name is corrected because it falsely claimed environment credentials while using memory storage. See the test-ownership rationale in `design.md`; native release acceptance and global module-depth review remain separate tasks.
- [x] 7.4 Enforce dependency direction so product packages cannot import repository tooling, tests, Forge code, ETHOS internals, or another client's implementation.
- [x] 7.5 Complete current consumer and redundancy review across product, tooling, tests, configuration and documentation. The current inventory covers 443 active carriers and 582 historical OpenSpec carriers; six native OS/architecture selections account for all 141 production Go files and 59 packages with no unconsumed product package, unused internal import allowance or forbidden edge. Every direct Go/npm dependency has a current consumer. Native duplicate/unused checks pass at the adopted policy. All remaining lower-threshold findings were reviewed by responsibility: distinct admission rules, native operations, archive boundaries and platform fixture inputs stay separate; five genuine repeated test owners were consolidated with all ten cases and six discriminating fault injections preserved. Existing ordering, JSON, transaction and projection owner reductions remain effective. Source and all required native jobs pass at signed f317e6ec on GitHub 34762310220 and GitLab 6579. The review and exact carrier hashes are in `build/verification/f317e6ec1082c90aca075c40ed5f133f018fc207/consumer-review-conclusion.json`, with native import/dependency and scanner outputs beside it. This closes current source-consumer review, not future impossibility of redundancy, final output/ref retirement, installation or publication. The subsequent lifecycle review found that the detached-source checker enforced an invalid archive-before-integration premise. Its native consumer was not a reason to preserve it: the checker, dispatch and obsolete tests are removed while native OpenSpec validation and the complete source graph remain. Current deletion evidence is under `build/verification/144b687b4c6d5fce81882c4efe4983a942e3d96c/delivery-lifecycle/`; final hosted admission remains under 13.6.
- [x] 7.6 Complete the root and `.config` ownership review. Tracked root and `.config` files have an intentional consumer and placement under [the authority map](../../../docs/governance/change-and-release-policy.md#authority-map). Native discovery stays at root; `.config` carries policy data, implementation stays in tools, and ETHOS owns generic hooks and coordination. The defaults-only Prettier file and seven redundant Taplo options are removed without changing formatter output; `.cbmignore` is corrected to development-tool ownership. Real TOML conformance preserves authored key order, array order and multiline grouping. Full rule effectiveness, hosted hooks, supply freshness and whole-repository topology remain under their own open tasks.
- [x] 7.7 Prove tracked entity count and duplicate ownership decrease while every required invariant retains one reachable implementation and test.
- [x] 7.8 Complete the module-depth review across synchronization, setup, client projection and verification, credentials, upgrade, and repository tooling. The [topology design](design.md#semantic-topology-precedes-physical-movement) records one owner for each retained invariant and recovery boundary. Removed forwarding and caller-coordinated recovery are covered by affected-client, credential, projection, workspace and signed-Git regressions. The final shared-writer correction retains write/close/cleanup causes and closes each handle exactly once. Full repository race and quality gates pass; complete physical topology, quantitative quality, hosted integration and final native product journeys remain separate acceptance tasks.

## 8. Quality authority

- [x] 8.1 Enforce unique carrier responsibility and effective native-check coverage without a duplicate check inventory. All 438 current and 582 archived tracked carriers resolve to one responsibility; the existing execution graph owns applicable native format, schema, Go, architecture, security and documentation checks. Cross-checkout conformance proves tracked-ignored inclusion, foreign-index isolation, complete pipe input, missing/empty inputs, source preservation, structural boundary pairs, warnings and first-failure propagation. Failed gate progress prevents execution; failed publication reporting preserves completed external effects without retry or rollback. At `1e3e2f18`, all 453 owner tests/subtests pass with race detection and no skips. [GitHub run 34710571357](https://github.com/HengYangDS/aigw-cli/actions/runs/34710571357) and GitLab pipeline 6515 pass at that exact commit; Windows logs confirm the CI, projection, architecture, coverage and Forge owner packages actually execute. Hook/hosted admission remains task 8.10, cross-concern security/release review remains 8.11, and visual, live-Provider and installed-artifact claims remain in their existing tasks.
- [x] 8.2 Consolidate Go format, vet, static analysis, modernization, naming, documentation, error, logging, security, complexity, import, and architecture policy into comprehensible owners.
- [x] 8.3 Adopt mature tools for dead code, dependency hygiene, vulnerabilities, secrets, licenses, SBOM, Markdown format and lint, links, TOML, YAML, JSON, CUE, Actions, and GitLab syntax where they provide net value.
- [x] 8.4 Remove custom generic checks superseded by mature tools and retain custom checks only for explicit AIGW product or repository invariants.
- [x] 8.5 Reassess structural limits by native measurement, protected risk and cohesive examples without exclusions, weaker assertions or count-only extraction. Native Go and size policies own the adopted bounds; the [calibration decision](../../../docs/governance/change-and-release-policy.md#calibration-decision) retains their rationale without duplicating trial counts or task status. Lower trials remain measured alternatives, not accepted floors. Source-bound trial inputs and final macOS/Linux/Windows-selected diagnostics remain under `build/verification/a73348e2a5bca9249faac08164f1b06aa840ab35/quality-calibration/`; subsequent cyclomatic trials are under `build/verification/204481c889102413e154b91cbbe507b73ab7cc3e/quality-calibration/`. Deleted Codex single-caller preparation and duplicate convergence logic preserve ordered artifacts and unchanged-file behavior; native mutation and race tests prove it. Global deletion, gate-effectiveness and performance acceptance remain distinct obligations.
- [x] 8.6 Require useful public and repository-tool documentation without ornamental test comments; verify generated command and configuration documentation cannot drift from source.
- [x] 8.7 Make repository-owned warnings fatal across development, test, build, docs, packaging, native artifacts, and CI; remove all current warnings at their owners.
- [x] 8.8 Enforce the selected revision's Conventional Commit grammar through installed ETHOS hooks and both Forge quality paths, including release-preparation subjects. The current hook passes nine admission cases without editing their input. All 49 introduced commits from accepted dev to 29dfa9ee verify identity, SSH trust and complete subject matching locally and in GitHub quality job 103700379310 and GitLab quality job 37713. Existing signed-object and CUE event tests pass, covering review, accepted-branch, tag and manual verification inputs; release tag checks share strict SemVer. Evidence: `build/verification/29dfa9eeb07f07f0557a8fbb6f38cd5ad3e6d0f4/` (`current-commit-hook.json`, `candidate-commit-range-current.log`, `github-quality.log`, `gitlab-quality.log`, `governance-conformance.log`). Actual integration, tag publication and source-branch deletion remain in 10.7–10.9 and 13.6–13.7, not implied by rule conformance.
- [x] 8.9 Run focused format, lint, type, architecture, dependency, security, docs, configuration, and behavior gates on each migrated owner before one final full suite.
- [x] 8.10 Verify native rules, scopes, exclusions and failure propagation through local commands, hooks and every required CUE event route. At signed `f07becef`, the existing CI, projection, Forge and repository suites pass with race detection; both quality jobs verify all 50 introduced commits. GitHub run 34749159196 and GitLab pipeline 6569 pass their required jobs, and the Windows log confirms actual execution of the repository-tool packages. Existing conformance covers checkout-bound inventories, tracked-ignored inputs, native schemas, complete stdin, warning/error propagation and review, accepted, tag and explicit-manual routing. Installed hooks remain active. The public ETHOS ref-update observer admits the exact proposal and rejects that same unarchived tree for dev with `openspec_ref_active_change_unarchived`; neither observation changes refs. Evidence: `build/verification/f07becef84afa9dc99e2c4c87cfc00c6ebe572d3/` (`governance-scope-conformance.log`, `github-quality.log`, `gitlab-quality.log`, `github-windows.log`, `ref-update-proposal.json`, `ref-update-unarchived-dev.json`). Actual accepted-ref integration, final-candidate qualification and published bytes remain under 10.7–10.9 and 13.4–13.6; gate conformance does not imply those effects.
- [x] 8.11 Complete the cross-concern review through the existing quality and release owners. Native source checks cover package-observed coverage, Go correctness/dead code, dependency locks, secrets, documentation, configuration and generated CI; each retains its existing execution authority and scoped conformance. The review corrected discarded output errors, undeclared gate network/output effects, OSV fixed-version and exact-lockfile attribution, release-source admission, publication credential leakage and single-platform SBOM coverage. SPDX normalization now binds all six native binaries to root-confined actual bytes, resolves scanner-root path semantics across hosts and preserves SHA-256 integrity checks; OSV rejects incomplete source observations before signing. Signed `e5488395` passes [GitHub run 34716437982](https://github.com/HengYangDS/aigw-cli/actions/runs/34716437982) on macOS, Linux and Windows plus GitLab pipeline 6521. The full source gate reports 97.38%; construction and publication conformance prove failure propagation, accepted-output preservation and owned cleanup without platform skips. Two real builds yield twelve byte-identical files. Independent archive extraction matches each SBOM binary digest, nine provenance subjects, source/tree and locks; the trusted signature verifies, with 43 Go and 286 npm package observations. Earlier single-binary and Windows-failing evidence is superseded, not counted as current acceptance. No parallel registry or SBOM merger was added. Hook/integration enforcement, global consumer deletion, rendered UX, live-Provider acceptance and final published installation retain their separate open tasks.

## 9. Development environment and supply chain

- [x] 9.1 Add minimal cross-platform `mise` tasks named `bootstrap`, `check`, `native`, and `release`; each delegates to existing ecosystem owners and no shell wrapper or parallel task runner remains.
- [x] 9.2 Prove a fresh Work Lane reconstructs independent dependencies, build, coverage, and temporary state from committed locks while sharing only content-addressed caches.
- [x] 9.3 Remove ambient interpreter, user-site, global mise configuration, system package, sibling checkout, and unpinned network discovery from successful local and hosted paths. Native mise, Go and npm contracts bind checkout-local dependencies; Syft and OSV consume explicit native policy. Root/nested local and Forge conformance preserves foreign inputs. The shared early mise policy replaces duplicate CI environment overrides. Hosted cache creation and matching-key reuse are evidenced by GitHub runs 34693876023 and 34718230705, and GitLab jobs 37342 and 37469; cache-fill success does not imply the earlier whole pipelines passed. Scanner correction 8e42a8a3 passes GitHub 34719966717 and GitLab 6530. Unified discovery source 660465e8 passes GitHub 34720745421 and GitLab 6531, including native macOS, Linux and Windows; deliberately supplied process overrides remain outside arbitrary-shell isolation.
- [x] 9.4 Audit every direct runtime, development, OpenSpec, Go, Node, mise, packaging, documentation, CI Action, and release dependency against current stable releases. The current refresh also rechecks scoped npm overrides: jsdom 30.0.1 remains unadmitted because its advertised whatwg-url 17.1.1 provenance endpoint still returns HTTP 404; the existing documented exception is not represented as latest.
- [x] 9.5 Advance admitted stable dependencies in their existing owners and regenerate Go, npm, and mise locks deterministically. Fresh native queries find no newer declared tools, direct npm packages or direct Go modules. CI mise advances to 2026.9.7 and Renovate to 44.83.2 using their independently resolved immutable OCI index digests. CUE regenerates both Forge projections; its stale projection fails before regeneration and passes afterward. The new mise image executes on Linux arm64; Renovate's strict validator and isolated offline extraction pass with 71 dependency observations and zero warnings. Focused CI/projection/repository race tests, native TOML and format checks pass. Go/npm/mise locks remain unchanged because their tool graphs did not change; the compatible local minimum remains 2026.9.5, not a second current-version pin. Shared-host mise upgrade belongs to the workstation owner and is not inferred from CI configuration. The three-day automatic-update policy remains intact; these tool updates are deliberately admitted. The jsdom/whatwg-url provenance endpoint still returns 404, so that documented exception remains explicit. Evidence: `build/verification/6212ed71b096d053738e6a61c40903ff6e64af11/supply-refresh/`. Current-commit hosted acceptance remains separate.
- [x] 9.6 Requalify the updated supply chain on macOS, Linux, and Windows: verify every executable and the OSV compiler, then resolve each native lock twice without changing source bytes. Full native qualification 34810123835 passes at signed f7f9ac99 with mise 2026.9.7. The three downloaded native locks equal the tracked lock, SHA-256 `5c15954df39a02e829383e264d971a4fbece9afb818d646fcc32b222581aea71`; each native job executes the full declared tool graph and repeated lock resolution. Toolchain, dependency, bootstrap and CI inputs are unchanged through rc.114 candidate 6d720d91, whose ordinary three-platform review 34817738090 also passes. Evidence: `build/verification/f7f9ac9958648f31900913b40484ef9172b6a390/native-locks/` and the exact hosted runs. This closes supply qualification, not later product installation or an unadmitted dependency exception.
- [ ] 9.7 Configure one dependency-update proposal owner with release-age policy, safe grouping, vulnerability priority, and object-preserving automatic merge; prove a live signed proposal refreshes native locks and CI projections, passes checks, merges once, and creates no competing peer proposal. The native policy, one-peer deployment boundary, release-age rules and scoped extractor are implemented and tested. Renovate 44.83.0 reproduces the 71 authored dependency observations with read-only repository access, no network, no credentials and no warnings. Remaining: activate the single operator-owned maintenance runner and prove one real signed update, lock/projection refresh, required CI, object-preserving merge and proposal retirement. Offline extraction is not scheduling or automatic-merge evidence. The delivery-inclusive OpenSpec lifecycle gap also affects truthful integration; do not create a competing updater, second progress ledger or parallel proposal to claim closure.
- [x] 9.8 Generate and verify SBOM, vulnerability, license, checksum, signature, and provenance outputs without checkout paths, volatile run timestamps, credentials, or installer-local metadata; derive any format-required time from the release identity.
- [x] 9.9 Evaluate the current Go release construction against maintained alternatives only where measurements show meaningful gains; avoid replacing the native Go toolchain with a second packaging framework.

## 10. CI and dual-Forge projection

- [x] 10.1 Define the complete semantic CI graph in CUE: fast quality, Go matrix, native source evidence, artifact construction, lifecycle acceptance, release metadata, publication, and parity.
- [x] 10.2 Generate GitHub Actions and GitLab CI from that model; reject hand-edited drift while preserving only provider syntax and runner-capability deltas.
- [x] 10.3 Cover proposal creation and update, review commit, maintainer fast-forward, direct `dev` push, `main`, and tag events with required exact-commit evidence; close the direct-`dev` trigger gap on both Forges. Review admission now covers both integration and release targets, including `dev` to `main`; GitLab workflow and job admission share the existing CUE rule set. Projection regressions cover both default and renamed branch roles, required native jobs and the post-push parity boundary. Hosted execution and merge acceptance remain under 10.7–10.9.
- [x] 10.4 Separate independently useful jobs and parallelize safe matrices; remove monolithic verification and duplicate jobs that prove no additional fact.
- [x] 10.5 Define exact conditions for evidence reuse and require rerun when source, platform, environment, lock, toolchain, release, or claimed fact changes.
- [x] 10.6 Assign Windows product proof to an available native runner without requiring a symmetric but unavailable GitLab Windows runner.
- [x] 10.7 Verify GitHub and GitLab authentication, SSH transport, author identity, commit and tag signature, protected branches, unprotected proposal branches, automatic merge, and source-branch deletion without password prompts. Both peers accept exact signed `4161ed5b`; PR 33 and MR 55 are merged, their proposal refs are absent, and main/dev CI passes. GitHub main/dev now strictly require the three native acceptance checks and Quality and governance from GitHub Actions, with signatures, linear history and administrator enforcement retained. PR 33 was BLOCKED while checks ran and CLEAN only after success. GitLab retains pipeline-required fast-forward merge and automatic source deletion. The admitted maintainer path preserved the signed object without human confirmation or weakening protection. This proves the executed integration, not the scheduled dependency-update automation in 9.7. Evidence: `build/verification/4161ed5bfbd299ca4156350d176727f8df332b74/branch-admission/`.
- [x] 10.8 Repair AIGW GitHub `main` and `dev` lag using the exact locally accepted signed objects; verify both Forges host identical Git object IDs and no Forge rewrites identity. Exact signed 07bc257d and tree 14c5e696 advance local candidate/dev, dev and main from 36a6a283 through one native Git CAS transaction; clean accepted and candidate worktrees match that tree. Both independent peers atomically advance main/dev to the identical commit with observed-old-object leases. All 78 introduced commits verify against explicit trust, exact full proof dc5a99c8917effd0254faa7858a0b8e1817c1f25c0255f4fd6b708b682f65b42 passes, and review GitHub 34790330060 / GitLab 6608 pass. The bounded maintainer recovery waives only the confirmed archive-before-acceptance defect; persistent hooks, active Change, formal installation, rollback and user settings are preserved. Evidence: `build/verification/07bc257dbdd776cb4ec003bf2f13273c6d605019/integration/`. Accepted-branch CI, release assets, final installation and retirement remain separate delivery obligations.
- [x] 10.9 Keep one proposal per Change, update its exact commit, require green evidence, merge it by the admitted developer or maintainer path, and delete the proposal ref promptly. GitHub PR 31 and GitLab MR 53 both report merged at exact 07bc257d after the authorized object-preserving maintainer integration; neither Forge constructed a replacement commit. GitHub removed its source branch, and the remaining GitLab proposal was deleted through passing public ref admission and exact-lease Git push. Both remote proposal refs are absent; main/dev readback remains unchanged. Evidence: `build/verification/07bc257dbdd776cb4ec003bf2f13273c6d605019/integration/` (review readback, deletion admission, push and final refs). The active authoring lane and its unfinished tasks remain; local stale-checkout retirement and future review work are not implied.

## 11. Documentation and user experience

- [x] 11.1 Review documentation ownership by responsibility and audience: research, adopted decisions, current architecture, concepts, user journeys, operations and contributor policy. Correct mixed-purpose documents and references without adding empty indexes.
- [x] 11.2 Make every directory index purposeful, remove empty or redirect-only README files, and ensure internal concepts, commands, decisions, evidence, and stable external standards use valid descriptive links. The shared-reference correction removes two private research retrieval paths and links configuration, source ownership and contributor authorities to their committed owners. All 22 canonical documents are reachable from the documentation map; the remaining literal `go.mod` is a filename-grammar example, not an authority citation. The link owner reuses native lychee extraction and validates local targets against the Git index; regressions distinguish untracked, ignored, tracked and staged directory targets while preserving long stdin inventories and heading checks. A tracked-only checkout passes local file and fragment verification, full source passes at 97.08%, and rc.114 native CI passes on macOS/Linux/Windows. Evidence: `build/verification/872c507a9792dc883779a674595af587479a6043/shared-documentation/` and review 34819179872. External reachability, hosted rendering and full reader-journey accessibility remain separate under 11.8.
- [x] 11.3 Rewrite setup, team manifest, credential backend, client-later-installation, direct endpoint, optional external-gateway composition, recovery, and uninstall journeys in precise natural language.
- [x] 11.4 Complete the source-derived CLI reference and behavior audit. Cobra/pflag remain the only command and option definitions: all 45 entries (44 public) and 105 option occurrences render from source at wide and narrow widths. Current `ee0cfe7a` passes 975 CLI tests/subtests and the full source gate at 97.36% statement coverage. Its trusted signed macOS artifact passes 178 isolated process invocations, including 23 inspected JSON results and 16 expected rejections: setup with deferred clients, environment/file credentials, actual helper output, Account/Profile/Route management, optional balance diagnostics, guarded import, idempotent use, rename/finalization, backup recovery, install and uninstall. Existing acceptance owns interactive prompt contracts, injected dependency failures, error rendering and output failure propagation. Actual Codex 0.154.0 and Claude Code 2.1.236 pass controlled-upstream streaming through predecessor, candidate, rollback and re-upgrade, followed by enabled-client bulk verification, exact Account retirement and uninstall. Review corrected supported-versus-enabled scope in verification, retirement and required Token queries; token-free/client-free team setup now passes doctor without inventing an installation failure. Existing checkpoints still bind current configuration; no source file, checkpoint format or lifecycle framework was added. Source-bound inventories, source tests, process transcripts, artifact trust and scope limits are recorded in `build/verification/f7667a8cbbf8bfce1af01ce8cc042a8582697b3b/cli-completion/cli-audit.json`. This is command-contract acceptance, not live Provider availability, complete rendered documentation, current three-platform publication or installed-product replacement; those remain under 11.5–12.9 and 13.6.
- [x] 11.5 Normalize semantic organization and presentation across code, schemas, manifests, docs, help, and generated projections. Current review builds on the completed carrier/consumer inventory and rechecks all 47 authored Markdown files at 390/1440 pixels. Three retained Mermaid diagrams preserve their actors, edges, accessible titles and descriptions at readable native size. Long prose tables become scoped lists or project sections; comparison tables remain where readable. All 26 competitor entries, their capability/strength/limitation cells, source links, policy values and team metadata remain intact. Seven changed documents lose 20,354 bytes of padding and duplicate mappings without adding a file, dependency or second authority. Local page/table layout, headings and link labels pass; original link destinations remain, with three explicit owner links added. Native team naming/purpose/recommendation/export, command metadata and formatting-scope race tests pass, as does the complete quality graph. Evidence: `build/verification/345275552b51ce5202528be318ec1bd71d1594b0/document-render-after.json`, `document-table-transformation.json`, `document-link-preservation.json`, `document-consumers.log` and `document-quality.log`. This closes source presentation and cross-format ownership, not authenticated Forge rendering, live recommendations, final released-byte or fresh-user acceptance under 11.6, 11.8 and 12.6.
- [ ] 11.6 Deliver one reviewed directly consumable team profile with current provider identifier evidence, authenticated protocol and real-client acceptance for recommendations, no credentials in version control, and safe local overrides; distinguish configured coverage from verified availability. The released rc.112 team journey covers no Token and independently connected AIHubMix, DMXAPI and UCloud using the explicit published candidate. Full native run 34794753857 and the macOS/Linux real-client journeys pass. Missing candidate input fails instead of substituting a source build. Evidence: `build/verification/37263dca577e1806698c3c1b097d191df6dcba02/publication/`. Remaining: current authenticated recommendation proof and durable team distribution. Controlled streams do not establish live Provider availability.
- [ ] 11.7 Remove stale claims, contradictory instructions, historical workarounds, compatibility notes without consumers, and evidence directories that do not own durable product facts; retain policy only under its actual audience owner and verify claims against their sources. The complete projected nine-capability contract has been inspected across routing, setup, credential storage, client ownership, CI, quality, repository structure and delivery. Earlier corrections are retained under `build/verification/8918e18c87f209da05d7ae99cfe62cc3cbe9e87c/spec-reconciliation/` and `build/verification/c13709a3e174edbed7000726a7f22a53d9eb9606/spec-selection-review/`. The current batch replaces obsolete mirrored-tool and HTTP-version prescriptions, separates work/proposal validation from accepted-ref admission, removes self-referential evidence expectations, defines setup completion after all client projections, scopes local projection paths and credential metadata claims, and makes recovery failures explicit. One duplicate onboarding requirement is absorbed into its existing result owner; all 388 scenario names remain across 116 rather than 117 requirements. Official merge is deterministic with zero errors/warnings; 25 long-body INFO advisories remain disclosed rather than driving artificial requirement splits. Existing CI, synchronization, credential and coverage owner race tests pass. Evidence: `build/verification/882ce29b435681c7b06277f28ca94fd22d8074f4/contract-reconciliation/`. Remaining: final current-document/hosted claim review and admitted canonical application; semantic review is not proof of runtime completeness, and canonical specs remain unchanged until closeout.
- [ ] 11.8 Verify explicit links and actual Markdown rendering, including Mermaid semantics and syntax, tables, code languages and copyability, lists, heading navigation, accessibility, terminal layouts, JSON stability and executable fresh-user journeys. Current released source `2946d0ae` has 22 actual GitHub observations for all 11 changed Markdown carriers at 390/1440 pixels; API bytes match Git, heading counts match source, and no article overflow, unnamed link or missing image alternative is observed. Native lychee reports 337 successful references, 209 unique, zero errors/timeouts/exclusions and one redirect across 47 current Markdown files; 13 internally cached observations are disclosed. Visual review exposed GitHub Mermaid controls obscuring the endpoint node at narrow width. The redundant three-node diagram and its duplicate explanation are removed, retaining the complete configuration/traffic boundary in product-position prose rather than adding renderer-specific controls. The source change still needs hosted readback. The native hosted code-copy element is present; clipboard activation and complete accessibility are not established by DOM inspection. Evidence: `build/verification/2946d0aed02ce52dbf9d5ce63b5a047a8f43d7a3/hosted-documents/` and `build/verification/a51e03fe9e50a6bb9307bba11be3465c2e793bb7/documentation-acceptance/`. Remaining: changed-source hosted review, authenticated GitLab layout without credential prompts, full accessibility and published-byte fresh-user/client limits under 12.6. Local preview or API HTML is not authenticated GitLab rendering.

## 12. Cross-platform product and release evidence

- [x] 12.1 Prove clean-room source bootstrap and the full fast gate on macOS, Linux, and Windows from the exact candidate commit. Signed `9b784fcf8f0920a202405562ba27c27e72b028e8` passes [full native qualification 34738120215](https://github.com/HengYangDS/aigw-cli/actions/runs/34738120215): each clean hosted checkout installs the locked toolchain, bootstraps dependencies and executes the same complete 26-command quality, behavior and packaged-lifecycle graph. Native Windows also passes system credential storage. Ordinary GitHub run 34738077066 and GitLab pipeline 6558 pass at the same source. The first full Windows attempt at `feb76045` exposed the complete Markdown inventory exceeding the process argument limit; the existing link owner now uses native `lychee --files-from -` through the existing stdin executor. RED/GREEN covers 400 paths, exact scope, real link checking and failure propagation without a platform branch or new carrier. Raw job logs, API results and regression output are under `build/verification/ec7c732493b27f0bb561eb59676120265dfbcf34/native-quality/`. This closes source/toolchain platform qualification at that exact commit, not live Provider inference, final release-asset identity or later-source final acceptance under 13.4.
- [ ] 12.2 Build immutable native artifacts for every declared OS and architecture; verify embedded version, checksum, signature, SBOM, provenance, startup, help and absence of checkout dependencies. Released rc.114 has matching signed twelve-file matrices on both peers and passed published-byte macOS arm64, Linux amd64 and Windows amd64 qualification. Native Windows ARM64 run 34833858425 now also passes exact-tag trust and artifact verification, lifecycle/rollback, late client/Token activation, Windows Credential Manager and the actual team manifest. Its executed program SHA-256 `b97ce48fbb6d186355508d2d0fdf405e82ce875ecf8bbfbc7dd5cc589b711729` equals the independently downloaded published ARM64 archive. The existing read-only workflow retains exact-tag source and locks, synthetic hosted credentials and the existing lifecycle owner; no candidate rebuild or additional workflow is introduced. Evidence: `build/verification/48bb687e28e15eb778d0d695b1a2d05932b02e46/native-release/`. Remaining: run the current published Linux arm64 and physical Intel macOS archives; older Linux arm64 and Rosetta results are not current native qualification. Real-client inference and supported context limits remain under 12.6 and 12.7.
- [x] 12.3 Prove fresh install, status, setup, use, sync, check, doctor, update, rollback, recovery, disable and uninstall on macOS. The signed candidate passes packaged upgrade/rollback and real Codex/Claude journeys; a separate isolated 17-command matrix proves client-scoped selection, repeated-use byte preservation, configuration rollback and repeated-repair terminality. [Native hosted qualification](https://github.com/HengYangDS/aigw-cli/actions/runs/34695175091) proves Keychain and published-predecessor lifecycle on an ephemeral host. Operator installation and configuration remain byte-identical. Formal publication and installed-product replacement remain under 13.6.
- [x] 12.4 Prove the supported product journey on native Linux, including Secret Service and headless fallback. The current Linux arm64 program passes delayed client/Token activation, one connected Account, packaged upgrade/rollback/uninstall, native Secret Service continuity and no-session-bus file fallback as an unprivileged user. Native [GitHub qualification](https://github.com/HengYangDS/aigw-cli/actions/runs/34695175091) and [GitLab qualification](http://192.168.64.101:18086/dig/misc/tools/llm-third-party-api/aigw-cli/-/pipelines/6502) also pass. Exact program bytes, container isolation and cleanup are recorded with the source-bound native evidence. Live Provider inference, final Release assets and installed-runtime claims remain separate obligations. The documented upstream GNOME Keyring replacement warning is not suppressed or represented as an AIGW credential duplication.
- [x] 12.5 Prove the supported product journey on native Windows, including Credential Manager, path conventions, process environment, recoverable program replacement and exact cleanup. Source `9dec1b6d` passes [native Windows acceptance](https://github.com/HengYangDS/aigw-cli/actions/runs/34694623110) and [historical artifact and real-client qualification](https://github.com/HengYangDS/aigw-cli/actions/runs/34695175091). The native suite covers UNC and extended-length derivation plus transient and persistent source/destination handle locks; the packaged journey preserves credentials and user files through upgrade, rollback and uninstall. Final published-asset identity remains a separate release obligation in 12.2 and 13.6.
- [ ] 12.6 Prove real Codex CLI and Claude Code projections from the released artifact, including delayed installation, invocation outside the installer shell, reasoning settings and supported context limits. Published rc.113 run 34806943366 passes full native tool qualification and retained rc.112 lifecycle on macOS arm64, Linux amd64 and Windows amd64, including real Windows clients. Local Codex 0.154.0 and Claude 2.1.236 pass baseline, upgrade, rollback, re-upgrade, preservation and uninstall with the same published bytes. The first local run failed final cleanup because Codex's unrelated plugin synchronization wrote into its temporary home after client exit. The existing fixture now explicitly disables plugins only in that temporary home and checks preservation of that choice; RED/GREEN input contracts and the complete client journey pass. No operator client preference changed. The exact failed scratch is removed after process and open-handle checks. Earlier isolated Linux arm64 client evidence remains under the rc.112 publication record. Current evidence: `build/verification/9bdb2b4aad69934289ba946127eb19e5ff88d7ca/publication/`. Controlled streams prove client integration, not live Provider availability or actual context capacity; supported-limit evidence remains open.
- [ ] 12.7 Prove direct Provider endpoints and an explicitly selected external compatibility endpoint without giving AIGW ownership of the external service. The public endpoint command accepts a bounded stdin Token and an absolute configuration file without credential-store reads or writes; the explicit go-keyring-base64 format decodes the pinned macOS storage envelope once. This input path shipped in rc.113 and remains in released and installed rc.114, whose real-client and lifecycle acceptance passes. Earlier production attempts returned relay code 23 before consumer startup; no Token invalidity or model inference follows. Those old attempts are not evidence of current helper failure. The helper now reports safe diagnostics; its installed synthetic probes and 24 independent private-Keychain checks do not authorize or prove production credential access. Current same-thread DMX requests return HTTP 200, but complete direct/selected-endpoint and recommendation acceptance is still missing. Remaining: supported noninteractive production credential authorization and explicit real-Provider/model acceptance without retries, ACL changes or fallback reads. Evidence: `build/verification/c38eb2ee0474de57ee6b87e7e358fb19a9f84a24/provider-consumer/` and `build/verification/2946d0aed02ce52dbf9d5ce63b5a047a8f43d7a3/installation/`.
- [x] 12.8 Prove native update from the retained published predecessor, failed-successor rollback, repeated recovery terminality and exact uninstall on every supported platform; preserve credentials and unowned state. Published rc.112 run 34794753857 passes exact-byte product, rollback-configuration and shipped-team journeys against retained rc.110 on macOS arm64, Linux amd64 and Windows amd64. The native lifecycle checks invalid-successor preservation, repeated repair byte-stability and exact uninstall; Keychain and Credential Manager continuity pass. An additional network-isolated Linux arm64 run executes the same published bytes against rc.110 through Secret Service rotation/update/rollback/re-upgrade/uninstall and the no-session-bus secure-file fallback. Its unprivileged ephemeral DBus/keyring exits cleanly, with no daemon warnings, test residue, surviving container or scratch. Hosted logs and published-byte identities are under `build/verification/37263dca577e1806698c3c1b097d191df6dcba02/publication/`; final Linux credential evidence is under `build/verification/c9ad7257ca40dd38d5e280edb5f7ed65b69972b9/linux-vault/`. This proves the declared OS lifecycle and credential boundaries, not live Provider inference or every CPU startup target.
- [ ] 12.9 Compare performance, memory, startup, binary size, projection latency, and credential access against explicit budgets and the retained baseline. The existing [performance policy](../../../docs/governance/change-and-release-policy.md#performance-and-completion-claims) declares timing budgets, memory/size review thresholds and uncertainty rules. Exact rc.112 versus retained signed rc.111 bytes pass macOS arm64 projection and environment-helper budgets: candidate/baseline p95 is 29.228/28.126 ms and 9.733/9.311 ms. Linux arm64 completes ten metric/backend pairs using environment, file and native Secret Service credentials; candidate p95 is at most 3.163 ms, including the native shell, and both forty-sample blocks meet budgets after five warmups. Six matching executable targets remain below both material-size review thresholds. Settings/sidecar no-op preservation and withdrawal pass; result files are hash-verified, exact scratch and the isolated container are absent, and operator hashes are unchanged. Evidence: `build/verification/4161ed5bfbd299ca4156350d176727f8df332b74/performance/final-projection/` and `build/verification/d81bf8a7bdab018d208d8510da9e48aede5bace2/performance/`. The rc.111 matrix is retained and signature-verified, not currently available as a hosted Release. Remaining: Windows timing, native macOS/Windows vault latency and Linux/Windows peak RSS. Synthetic endpoint validation is not Provider inference; no speed-improvement claim is made.

## 13. Destructive cleanup and terminal closeout

- [ ] 13.1 Re-run the semantic inventory and delete every obsolete implementation, migration path, schema reader, wrapper, alias, generated file, cache, runtime, temporary service, branch, tag, Release, or record with no current consumer. Public ETHOS retirement removes the absorbed, clean `verification-74a34d8` Work Lane, its local proposal ref and registration. Tree equivalence and ancestry prove absorption; current absence is verified. Four superseded candidate matrices, redundant expanded retirement output, three obsolete Node caches and three development binaries are removed. The exact native receipt, canonical published matrix, rollback programs, pending evidence and client-owned state are retained. Evidence: `build/verification/4161ed5bfbd299ca4156350d176727f8df332b74/retained-review-retirement/`. Earlier verified cleanup remains in Git history and its native evidence. Global owned runtime/ref/release cleanup and final lane retirement remain open.
- [x] 13.2 Verify `.gitignore`, attributes, editor settings, root metadata, `.config`, OpenSpec, docs, tests, tools, and package layout contain only current intentional carriers in stable logical order; keep `.serena`, `.code-memory`, and `.codebase-memory` host-local where present. Exact `6225484d` accounts for 1,024 tracked files: 442 current carriers and 582 immutable OpenSpec archives, including 19 native root files and 13 policy-only `.config` files. The earlier whole-tree review is reconciled with all four added carriers: explicit OSV and Syft inputs have native consumers and scope regressions; installation inspection and its tests live under the existing upgrade owner and serve the public installation command. All six OS/architecture import inventories resolve 59 packages and preserve product-to-tool separation. The architecture gate passes, current style gates and native ignore checks pass, and the existing ownership map governs placement. Volatile inventory counts are removed from design prose rather than copied into another registry. Source hashes, inventory delta, native imports and current gate output are under `build/verification/6225484d2fd718edb349d425fbd2e1e59dbb6ff0/organization-acceptance/`. This closes placement and carrier ownership, not the generic lifecycle behavior overlap in 7.5, hosted hook admission, output retirement or product release.
- [ ] 13.3 Verify every user feedback item in this Change maps once to an implemented invariant and exact evidence; apply recurring-failure lessons at their existing owner using the [repair closure](../../../CONTRIBUTING.md#closing-a-repair) contract, remove contradicted guidance, and disclose unverified outcomes without duplicating work.
- [ ] 13.4 Run all format, lint, type, architecture, dependency, security, documentation, configuration, behavior, native, release, and installed-runtime gates from a pristine candidate.
- [ ] 13.5 Verify signed clean candidate HEAD, exact tree, complete OpenSpec progress, current ETHOS proof, no P0 or P1 defect, and no warning or hidden skipped required gate.
- [ ] 13.6 Complete successor-version acceptance, OpenSpec closeout, object-preserving integration, identical dual-peer release publication, safe installation and observed operator-state preservation; do not mark future lifecycle operations done. Release source `2946d0ae`, tree `7a481ec7` and signed tag object `47ee5747` for v0.1.0-rc.114 are identical locally and on both peers. PR 38 and MR 63 merged without rewritten commits; proposal refs are absent. GitHub main/dev and asset verification pass; GitLab main/dev/tag pass. Published-native run 34821392673 passes the actual rc.114 bytes against rc.113 on macOS, Linux and Windows, including real clients; its superseded ordinary tag run is recorded as cancelled, not success. Two final-source builds produce identical twelve-file matrices; independent downloads from both peers pass hash, signature and provenance verification. Installed rc.114 has SHA-256 `ae4b5858d59c490b83455d4ca47a48c4c8a182d4b327ec9eced28bb0df719a7d`; rc.113 is the rollback program. Public update and repeated no-op preserve eight configuration/sidecar/backend files and the exact Proxy listener; sync dry-run reports both client projections already converged. The governance boundary remains incomplete: publication used exact-object maintainer CAS, not successful native pre-push. Trusted-baseline ref observation did not supply the missing accepted closeout effect; the earlier inference that only archive ordering remained was incorrect. Preserve that failure and effect evidence; ETHOS owns the repair. Persistent hooks, signatures and Forge controls are unchanged. Evidence: `build/verification/2946d0aed02ce52dbf9d5ce63b5a047a8f43d7a3/`, especially `installation/`, `publication/`, `published-native-terminal.json` and `publication-governance-limits.json`. Remaining: residual product acceptance, actual credential incident follow-up, genuine Change completion/archive, native governance closure and lane retirement.
- [ ] 13.7 Delete the merged proposal, retire the Work Lane, prune obsolete local and remote refs and owned runtime residue, and verify the repository family and installed product are clean and healthy.
