## 1. Inventory

- [x] 1.1 Re-query official stable release sources for direct Mise, npm, Go,
      OpenSpec, CI, security, documentation, and release dependencies.
- [x] 1.2 Identify only the newly stale direct owner and preserve explicit
      compatibility constraints with current evidence.

## 2. Refresh

- [x] 2.1 Advance the stale direct declaration and regenerate its native lock
      or generated projection.
- [x] 2.2 Resolve twice and prove the second result is byte-identical.

## 3. Acceptance

- [x] 3.1 Pass strict OpenSpec, bootstrap, dependency-policy, source, native,
      and release-construction gates.
- [x] 3.2 Commit the signed candidate, obtain exact-HEAD proof, integrate it,
      and verify local and both Forge `main` and `dev` refs and CI.
