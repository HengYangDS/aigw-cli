schema_version = 1
id = "change:windows-test-portability"
intent = "Replace the POSIX-only unreadable-directory test construction with a deterministic cross-platform proof of the package-topology read failure contract."
subjects = ["repository:aigw-cli"]
scope = [
  "tools/architecture/**",
  "openspec/changes/windows-test-portability/**",
  "openspec/changes/archive/*-windows-test-portability/**",
]
invariants = [
  "production_package_topology_behavior_is_unchanged",
  "native_windows_tests_exercise_the_same_error_contract_without_goos_skips",
  "the_test_failure_carrier_is_deterministic_on_supported_platforms",
]
acceptance = [
  "the_regression_is_observed_red_before_implementation",
  "focused_architecture_tests_pass_on_the_locked_toolchain",
  "exact_head_full_proof_passes",
]
risks = ["a_test_seam_could_expand_the_production_surface_without_need"]
authority_refs = ["user_instruction", "AGENTS.md", "tools/architecture/behavior_test.go"]
hypotheses = []
dependencies = []
