# Published GitHub release tags that have no GitLab provenance tag.  The
# Changelog validator uses this append-only source inventory when a GitLab-only
# checkout cannot fetch the private GitHub tag namespace.  Do not list a tag
# that exists in the GitLab namespace.
